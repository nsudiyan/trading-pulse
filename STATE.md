# STATE — единственный живой статус проекта

> Обновляется при каждом значимом изменении. Всё остальное — история.
> Последнее обновление: **2026-06-07** (P0-серия + P1-5 доки + P1-6 гигиена + P1-7 liqtracker).

## 1. РАБОТАЕТ

- **Скринер** каждые 4ч (00/04/08/12/16/20 UTC) + строка `funnel: candidates= passed= rejected= top_reason=` в каждом прогоне (`6c18d87`)
- **Алерты с кнопками** [✅ Вошёл / ⏭ Пропустил] → `outcomes/alerts_index.json` → callback → `outcomes/trades.json` (trade_logger, status=open) → `/close SYM ±R|цена`, `/open` (`1ab24f7`…`7a0887f`)
- **Macro-veto алерты**: враждебный макро при нормальном сетапе = WAIT с пометкой «⚠ МАКРО ПРОТИВ» в TG, решение за человеком (`7e6e21d`)
- **Obsidian fail-open**: запись заметок не может убить прогон (`099d27d`)
- **Heartbeat** каждые 6ч — сирена в личку при молчании скринера >5ч / 48ч passed=0 / отказе доставки (`e47945e`)
- **daily_report** 12:00 (cron починен) + блок «РЕАЛЬНЫЕ СДЕЛКИ» отдельно от paper
- **check_score_gate** 09:05; sector_heat */10 мин
- **Ротация логов** (вс 12:20) + **бэкапы** resolved/pump_resolved/trades (вс 12:40) — launchd-агенты, навёрстывают после сна (`41f8621`)
- **liqtracker**: тихие реконнекты (джиттер, сброс после 5 мин стабильности), лог ожил (был мёртв с 14.04 — basicConfig без stream); БД retention 14 дней, WAL чекпоинтнут 25МБ→0 (`4d2d097`)
- **Всё расписание на launchd** (cron пуст, навёрстывание после сна): dailyreport 12:00, sectorheat */10, heartbeat 0/6/12/18, rotatelogs/outcomesbackup вс (`4f5acee`); check_score_gate убран — one-shot, выстрелил 01.06
- **run_*.sh**: ранний cd + set -u — getcwd-баг закрыт; все плисты с абсолютным WD; ngrok отключён от автозагрузки (.disabled — включить при нужде в удалённом дашборде) (`4e981de`)

## 2. СЛОМАНО / МОНИТОРИМ

- **Воронку держат DistGate-C (31.8% отказов) + HardGate** (режет score 130–150).
  Решение — по статистике funnel-строк (копится с 2026-06-06, см. `tools/gate_forensics.py`)
- **Claude-фильтр в макро-дедлоке** до выхода из Extreme Fear (0 GO с 03.06).
  След для оценки «был ли прав»: `outcomes/shadow_verdicts.jsonl`, поле `macro_veto`
- **signal_monitor** — параллельный конвейер (рескан */10 мин + свой реестр active_signals): решение владельца ожидается (P1-8, блок 3)
- **Диск ~82% full** (−112МБ гигиеной; pump_analysis/ 532МБ не тронут) → iCloud всё ещё выгружает файлы

## 3. РЕШЕНИЯ ВЛАДЕЛЬЦА

- Система = **сканер для ручных решений**, не автосигналы
- Макро-вето → WAIT с пометкой (не SKIP); качественный мусор фильтр режет
- Реальный учёт — **только кнопки + /close** (никаких ручных правок trades.json)
- Новые фичи **заморожены до конца P1**

## 4. НЕ ДЕЛАЕМ ОСОЗНАННО

- `USE_VALIDATED_V1=False` — ждёт честных форвард-данных (не in-sample)
  (факторы описаны в archive/docs_2026H1/{PUMP,DUMP}_PATTERNS_ANALYSIS.md —
  комменты в pump_detector.py:368 / rug_detector.py:104 ссылаются туда)
- Распил screener.py (god-object 357КБ) — после стабилизации
- Доработки web_dashboard
- Мёртвые direction-гейты не трогаем (осознанно, см. LIVE_CODE_AUDIT)

## 5. АРХИВ

**archive/docs_2026H1/ — история (25 доков трёх эпох).
Статусам и «DONE» там НЕ верить.** Живой статус — только этот файл.
