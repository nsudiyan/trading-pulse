# STATE — единственный живой статус проекта

> ⚡ **С НОЧИ 11.07 ЗАВОД ЖИВЁТ НА СЕРВЕРЕ 147.45.175.24** (/root/trading,
> systemd trading-*, расписания МСК). Мак = редактор; деплой бота =
> ~/trading/deploy_bot.sh; дашборд Пульса = vercel из dashboard/site (ОТДЕЛЬНО);
> экстренный снос = /root/trading/eject_bot.sh. Правки файлов на маке НЕ работают
> в бою, пока не задеплоены. ⚠ Хвост миграции: GH_TOKEN для dashfeed-пуша
> (утренняя задача #12, руками брата).

> НОВОЙ МОДЕЛИ: сначала прочитай HANDOVER.md (паспорт передачи вахты).

> Обновляется при каждом значимом изменении. Всё остальное — история.
> Последнее обновление: **2026-07-10 ночь** — 🏁 ШЕСТИЛЕТНИЙ ПРОГОН ЗАВЕРШЁН,
> вердикт механический: **KILLED / NO-TRADE** (все 4 критерия §7 провалены;
> v2-архив под единой учётной моделью terminal-per-trade: full −597% t=−4.05
> @0.31, отрицательно во ВСЕХ годах и на ВСЕХ костах, bootstrap CI [−891,−303]
> без нуля; SHORT-зеркало −234 → после костов не торгуема НИ одна сторона;
> survivor-bias делает всё это ВЕРХНЕЙ границей; вердикт подтверждён Codex
> независимым пересчётом из trades.csv).
> studies/2026-07-10_six_year_{PREREG,DATA_MANIFEST,PARITY,ADVERSARIAL_MEMO,RESULTS}.md,
> данные/код: ~/trading/backtest_6y/ (конвейер = переиспользуемый актив).
> Радар остаётся ИНСТРУМЕНТОМ НАБЛЮДЕНИЯ, не источником сделок.
> ТЕСТ №2 ЗАПУЩЕН И ЗАКРЫТ ТОЙ ЖЕ НОЧЬЮ (брат отменил ожидание 20.07; предрег
> заморожен до данных, вето Codex вшито): **BTC-beta-hedged Radar → KILLED /
> NO-TRADE**, ещё жёстче лонга: alpha full −1130% t=−11.15 @0.31/ногу,
> gross-альфа ДО комиссий −0.168%/сделку — сигнальная монета ПРОИГРЫВАЕТ β×BTC
> (слабый маркер отрицательного отбора). Переворот не покрывает косты и был бы
> снупингом. ОБЯЗАТЕЛЬСТВО §8 ВСТУПИЛО: свечная ветка радара исчерпана
> ОКОНЧАТЕЛЬНО — дальше только механизмы (OI/flows) или иное исполнение.
> studies/2026-07-10_btc_hedged_{PREREG,RESULTS}.md · out/hedged_*.
> ТЕСТ №3 ПОДГОТОВЛЕН (Codex, ночь 10→11.07): H-OI-01 crowded leverage unwind —
> первый МЕХАНИЗМ (не свечи): OI↑ × односторонний funding → послед. слабость толпы.
> Черновик studies/2026-07-11_H-OI-01_PREREG_DRAFT.md с 4 вето-точками (age-cap OI,
> f_norm×кадентность — 318/609 монет 4ч!, score=f×relu(ΔOI), K=10); OI-история 4ч
> качается фоном → data/oi/. Априор честный: кост-стена 0.62%/день при 100% обороте.
> Заморозка и запуск — после вето Codex.
> ИТОГ ТЕСТА №3 (11.07, ПОСЛЕ as-of rerun): **H-OI-01 → KILLED / NO-TRADE чистый**.
> ⚠ Первый прогон SUPERSEDED: Codex нашёл same-bar look-ahead В ЗАМОРОЖЕННОЙ
> формуле (ts ≤ D + вход по open D; 21664/21664 позиций с OI ts=00:00) —
> обязательный bugfix (данные <D, вход 00:30), score/K/окна/ворота не тронуты,
> селфчек дополнен as-of регрессией. Чистый итог: net −611% t=−4.26 @0.31,
> все bootstrap-CI без нуля; НО gross/позицию +0.044% и лонг crowded-shorts
> +9.7бп/день (as-of правка сигнал УЛУЧШИЛА — механизм отскока толпы шортов
> реален, просто в 3–6 раз ниже кост-стены). Пост-хок срезы запрещены §5.
> ОБЯЗАТЕЛЬСТВО: класс OI-порогов ЗАКРЫТ; дальше ТОЛЬКО реальные forced flows
> (ликвидации, taker imbalance, order-book, межбирж. спред) = НОВЫЕ ДАННЫЕ.
> За ~30ч: 3 предрег-теста, 3 чистых KILLED (−597/−1130/−611).
> studies/2026-07-11_H-OI-01_{PREREG,RESULTS,RESULTS_SUPERSEDED}.md · out/hoi_*.
> 🌊 НАПРАВЛЕНИЕ №4 ОТКРЫТО (Codex, 11.07): H-FLOW-01 forced short-cover —
> торговать не ГДЕ толпа (знал OI, +9.7бп<костов), а КОГДА её выносят (лики+
> тейкер+книга). **Сборщик com.trading.flowcollector В БОЮ** (лики сырьём,
> тейкер 1м, книга L10/L50 1м, OI/funding 5м, топ-150 1ч, манифест дыр;
> ~30-60МБ/день; RLock-дедлок пойман тестом до деплоя). Черновик-структура
> prereg до исходов: studies/2026-07-11_H-FLOW-01_PREREG_DRAFT.md; калибровка
> порогов из распределений ПРИЗНАКОВ ~25.07 → заморозка → COLLECTING
> (вердикт не раньше N≥100 или 60 дней). Ретро бесплатно невозможно (ленты
> ликвидаций нет) — платная развилка Tardis = решение брата. ⚠ мак с крышкой =
> дыры forward-выборки → миграция демонов на сервер стала АКТУАЛЬНЕЕ.
>
> Предыдущее (2026-07-10 вечер) — консилиум Codex (ревью №2) принят: NO-TRADE до чистого пайплайна; tf-sweep = 0/50 ячеек (понижен до discovery, символ не сохранялся — ремонт невозможен, повтор = snooping); OI собран 18786/18786 (discovery-качество, нужен манифест); дашборд перелицован в «🧪 NO-TRADE» (локально, ЖДЁТ ДЕПЛОЯ); план = чистое ядро → PIT-вселенная → 3 prereg-теста (OI/Radar×Rose/market-neutral, train≤30.04 val=05 test=06.01–07.09). Studies: 2026-07-10_{tf_sweep_results,external_review_codex2}.txt · ответ Codex: ~/Desktop/pulse_for_codex/SWEEP_RESULT.md

## 10.07 вечер — вердикт консилиума и перелицовка дашборда (Fable)

- **Codex ревью №2 валидировано построчно** (studies/2026-07-10_external_review_codex2.txt):
  battle_year/parity/tf_sweep = discovery, не доказательства; порядок дальше принят.
- **Дашборд → честный NO-TRADE** (готово ЛОКАЛЬНО, деплой по команде брата):
  CSS-бейдж «ЗОНЫ ВХОДА — сделки берутся ТОЛЬКО отсюда» → «🧪 ИССЛЕДОВАТЕЛЬСКИЕ
  КАНДИДАТЫ — NO-TRADE» (зелёная рамка → янтарная); «🎯 Вход имеет смысл» →
  «🧪 Исследовательские кандидаты»; бейдж «▲ лонг» (план входа) → «🧪 наблюдение»;
  шорт-витрина вынесена из панели в отдельную секцию со статусом «ОПРОВЕРГАЕТСЯ»
  (год: funding+ шорты −1.22% vs −1.10% все, discovery); дневник: «вход» = виртуальный.
  Серверные правила log_entry_candidates НЕ тронуты — форензика копится тем же ключом.
- **✅ СДЕЛАНО РАЗДЕЛЕНИЕ наклона**: шапка теперь chips `v2 x/n · v3 x/n` раздельно
  (было: сумма 51% обманывала), тултип «когорты не смешиваются, НЕ prereg».
- **OI-фактор**: doi4/doi24 у 18786/18786 хитов battle_full.json. Направление PIT-верное,
  но raw/retCode/манифест не сохранялись → для prereg OI-теста пересбор с манифестом.
- **Прочее вчерашнее в силе**: stormreport TG-429 не чинён; бэкап off-site не сделан.
- **🏗 НАЧАТ ШЕСТИЛЕТНИЙ HONEST-БЭКТЕСТ боевого радара** (директива брата 10.07,
  спека Codex): PREREG зафиксирован ДО прогона (studies/2026-07-10_six_year_PREREG.md —
  параметры = продакшен `9e112767a763fd2e`, БЕЗ подбора; период 2020-04…2026-07-10;
  smoke на 2023-H2 чтобы не вскрыть test 2025+; вердикт только из трёх формул).
  Probe: делистнутые исчезают из Bybit API → survivorship-by-delisting неустраним,
  завышает лонги — потолок вердикта = CONFIRMED FOR PAPER ONLY. Качалка данных в
  фоне (~617 симв. × 30м с листинга + funding, задачи #1–6). ~/trading/backtest_6y/.

## 08.07 — работа Opus 4.8 (всё измерительный/дашборд слой, торговый не тронут)

- **Латентность фида 300с→~15-40с:** raw.githubusercontent Fastly держал фид до 5 мин,
  cache-buster `?t=` был мёртв (Fastly игнорит query). Фикс: `site/api/feed.js` — Vercel-прокси
  к GitHub contents API (токен `GITHUB_FEED_TOKEN` в env, no-store). Задеплоено. Детали: [[project_trading_pulse_dashboard]].
- **Дневник:** записи несут `side` (радар/пробуждение=long, связка из поста; шорт-связки СКИПАЮТСЯ —
  шорты не встраиваем, решение брата); вердикт «✅ в рамках»→«🎯 по прогнозу» (смешивал экзамен прогноза с PnL).
- **Шорт-тест панель** (просьба брата): справа от зон входа — витрина шторм-шортов с «лонги заперты»
  (funding≥+0.01), для глаз, НЕ сигнал/НЕ дневник.
- **🔑 К РАЗДЕЛЕНИЮ:** счётчик наклона в шапке СКЛАДЫВАЕТ v2+v3 (обманчив: v2 67%/n14 + v3 37%/n16 = 51%).
  Судить порознь (§7 n≥25/версию). Предложено разделить на `v2|v3` — ✅ сделано 10.07 (см. секцию выше).
- **🐛 stormreport exit=1:** Telegram 429 (сводка за 4 дня слишком длинная), последняя успешная 47ч назад.
  Данные целы (стейт не сдвинут). Кандидат на фикс (пауза между сообщениями). НЕ починено.
- **Бэкап невосполнимых данных:** outcomesbackup ✅ ловит diary/ledger/rose_history, но 🔴 на том же диске
  (off-site нет) + недельный ритм. Брат одобрил: ежедневно+KEEP14+off-site scp на 147.45.175.24. НЕ выполнено.
- **Переезд демонов на сервер = приоритет ПОСЛЕ боевого** (снять завод с мака). Простой мака 06-08.07:
  ≈10ч/3д, дыры от ЗАКРЫТОЙ крышки (не AC); ~25-35% молодой выборки дневника недобрано. [[project_trading_daemon_migration]].
- **KAITO 08.07** в реестр эталонов (пробуждение→приток по §7, но 1/10 меток, vol не предсказал магнитуду).

## 0. БОЕВОЙ РЕЖИМ 06.07 — что копится и когда жать

Конфигурация заморожена, новые правила в код НЕ вносим до вердиктов. Чекпоинты:

| форвард | где копится | порог решения |
|---|---|---|
| наклон v2/v3 (⬆⬇) | ledger.bias_at_birth → шапка дашборда | n≥25 на версию: 65%+ живёт, ~50% сносим |
| entry-кандидаты «вход сейчас» | outcomes/entry_candidates.csv (демон, дедуп) | ~20 закрытых суток → join с ledger: даёт ли воронка vs сырой пул |
| 🎣 иглы-выносы | feed.sweeps (плашки на карточках) | 2 нед: отработка после плашек vs ретро (43%/61%) |
| погодная плашка BTC (скринер) | визуал на карточках, разрез n=907 в studies/ | месяц: подтверждение 46/31 и 45/18 на новом окне |
| funding-лид вниз-пробоев (17% vs 7%) | storm_stages + Bybit funding | side_study-2 при удвоении down-ignite |
| связки пробуждение×Rose (×2 лучше, n=5) | feed.combos (самочистка по live) | n≥15 постов с пробуждением до |
| правила выхода (hold24/стопы/тейки) | треки копятся сами | ~2 нед / второй режим рынка → повторный бэктест |
| пробуждения ≥15× | vol_radar (мини-кап 3/день) | n≥10-20 |

Разрезы 06.07 сохранены: `outcomes/studies/2026-07-06_btc_regime_and_exits.txt`.
Правила «не сломай»: скринер-гейты (PumpWatchGate, ShortDistMute) и TG-доставка
не менялись; всё новое 06.07 — только дашборд/фид/форензика.

**📓 Дневник трейдера — В БОЮ (2026-07-07, вкладка дашборда #diary):** все
сигналы зон входа (вкл. неотработавшие): монета/базис/диапазон/итог 24ч.
Самообучение с тремя замками: (1) ожидание класса (квартили peak24)
ЗАМОРАЖИВАЕТСЯ при появлении в зоне — dashboard/diary.py, diary.json;
(2) вердикты (✅/〰/🚀/💥 по забору Тьюки p±1.5·IQR) только при n_class≥10,
иначе «⏳ копим»; (3) сюрпризы копятся в библиотеку со снапшотом (vol_ratio,
BTC-погода, возраст) но НЕ меняют правила — правила только через
side_study+форвард. Резолв в refresh_outcomes (15м), upsert в feed-тике (60с).
Холодный старт ожиданий из radar_resolved с пометкой src=proxy.

**ПОСЛЕ боевого периода — детальный план готов: `PLAN_POST_BATTLE.md`**
(фазы: вердикты → ретро-валидации дыр → side_study-2 → observe 🧪 → боевое →
деньги при ≥2 KEEP; у каждого пункта критерий отказа). Краткая выжимка:
- **Детектор дрейфа** — класс «+15–40%/24ч без вертикали и без vol-спайка»
  (щель между радаром и pump_watch; срез 07.07: радар взял 0 из топ-12
  гейнеров дня, шторм 4). Эскиз: ступенчатый рост ≥10%/12ч + объём 24ч
  ≥×2–3 своей 7д-медианы БЕЗ разового спайка + ΔOI ≥ 0 (не сквиз) + не мажор.
  ПОРЯДОК: сначала ретро-валидация на накопленных неделях (частота/день,
  отработка следующих 24ч vs база) — если пусто, НЕ строить (урок squeeze_radar);
  если живой — observe-режим 🧪 на дашборд, TG не трогать.
- Заодно закрыть «волну №2» радара (задранная 7д-медиана глушит повторное
  пробуждение: VANRY 06–07.07 дважды, BLUR) — например, медиана без топ-3
  бурных часов или отдельный порог для «после эпизода».

## 1. РАБОТАЕТ

- **pump-слом на 12с ignite-цикле (2026-07-06 вечер):** live_break_pass внутри
  ignite_loop (тикеры цикла переиспользованы, 0 лишних запросов) — 🔻-алерт за
  секунды вместо ≤10 мин лага WATCH-скана; без гонки: ignite шлёт+пишет очередь
  pump_ignite_breaks.json, скан подхватывает break_sent; пустой watchlist не
  усыпляет pump-надзор. Демон stormignite перезапущен.
- **Код-ревью окна 02–06.07 → 10 фиксов (2026-07-06, workflow 33 агента, все
  верифицированы адверсариально):** (1) bias-экзамен конвертирует signal-relative
  ret24 шорт-треков в мировой (иначе Rose-шорты зеркалились); (2) signals_ledger —
  оба писателя под file_lock, dashresolve МЕРЖИТ outcome по id (было lost-update
  каждые 15 мин, переякоренные скипаются); (3) tg-сигналы дашборда из
  rose_history.json (точный ts поста, стабильный msg_id) вместо mtime кэша скана;
  (4) недоставленные «пробуждения» больше не выпадают из обычной доставки;
  (5) кулдаун радара = EXPIRY: 4ч доставленным / 30м недоставленным (миграция
  старого формата на лету) — гига-спайк после недоставленного 6× больше не
  глохнет; (6) fetch_1m storm_report/radar_resolver: retCode≠0 → RAISE (транзиент
  ≠ делист), resolve() при пустых свечах → no_data/done_no_data вместо
  выдуманных «итог +0.0%», медианы только по реальным числам; (7) rose_watch:
  канал rose всегда LONG (синхронно с rose_history/фидом); (8) detect_spike:
  «цена ещё стоит» проверяется и по LIVE-цене (объём live-бара по-прежнему
  игнорируется — LA3/LA4); (9) seeded только при непустой ленте (пустой парс не
  «засеивает» канал); (10) окно сводки — с конца последней УСПЕШНОЙ (+кап 3д),
  фейл отправки не съедает сутки. Все селфчеки+юниты+гонка-тест PASS.
- **pump_watch — надзор «после вертикали»** (2026-07-03, из разбора TAIKO 01–02.07:
  +324%/3ч при OI −40% = шорт-сквиз → плато с OI +21% и funding −2.5%/ч = распределение
  → слом плато → ещё −68%). Модуль `pump_watch.py`, живёт ВНУТРИ WATCH-скана storm_radar
  (fail-open вызов в конце scan(), демоны не добавлялись, ignite не тронут). Три алерта
  фактами без прогнозов: 🌋 детект (рост ≥80%/4ч, kind по ΔOI: squeeze/trend/mixed),
  🌊 распределение (≥2 из: OI↑ от минимума плато, funding у лимита ≥1%, объём без хода),
  🔻 слом низа плато (close ниже; плато «вызревает» 2ч — фальстарты юного плато режутся).
  Стейт `outcomes/pump_watch_state.json` (TTL 48ч, cap 10), форензика в storm_stages.csv:
  pump_detect/pump_distribution/pump_break/pump_expire — lead-time считает только
  forward-резолвер. Бэктест на реальном TAIKO: DETECT 01:30 МСК (за час до поста Rose
  «x6», ×2.5 ниже пика) → DISTRIB 04:50 (за 4ч до дампа) → BREAK 09:50 на 0.3341
  (дальше −65%); BTC/BASED — 0 срабатываний. `python3 pump_watch.py --selfcheck`.
  **PumpWatchGate в боевом скоринге** (2026-07-03, решение брата): начало
  `_passes_setup_tg_filter()` (screener.py) — монета под живым pump-эпизодом →
  ЛОНГ-сетапы в TG не идут (шорты не тронуты). Направление — идиома setup_dir
  (НЕ r['direction'] — мёртвый ключ, грабли 2026-05-30). Каждый заглушенный
  сигнал — в rejected_history.csv (gate=PumpWatchGate, форензика/контрфакты A2).
  Fail-open: pump_watch недоступен → гейт пропускает. Тест: 5 веток (блок лонга,
  пропуск шорта/чистой монеты, fail-open) — без записи в боевую форензику.
  Наглядность: секция «🌋 Pump-надзор» на дашборде «Пульс» (эпизоды + заглушенные).
- **Дашборд «Пульс»** (2026-07-03): https://trading-pulse-mu.vercel.app —
  live-отработка сигналов (Bybit WS из браузера), Rose-треки 6ч/24ч, ledger итогов за
  сутки, аналитика бота (честное окно), storm/radar. Код `dashboard/`, фид пушится в
  GitHub (nsudiyan/mirofish-state/trading_feed.json) демонами com.trading.dashfeed
  (WatchPaths + 60с) и com.trading.dashresolve (15 мин). Подробности:
  memory/project_trading_pulse_dashboard.md.

- **Вирт. депозит ВЫКЛЮЧЕН** (2026-07-03): решение брата — бот сторону не берёт и в сделки
  не входит даже виртуально; отработка штормов = storm_report (%-хода). Рубильник
  `LIVE_ENABLED=False` в `calibration/virtual_account.py` глушит все TG-сообщения фичи:
  «📒 Закрыто N сигналов … 💰 Вирт. баланс» (notify_resolutions из screener.py) и строку
  «💰 Вирт. депозит» в алертах (deposit_line в telegram_alerts/pump_detector). resolved.csv
  пишется как раньше (нужен KB-аналитике); CLI-анализ virtual_account.py работает руками;
  rowcount двигается и выключенным — ре-включение не спамит историей. Демоны не трогал:
  эмиттеры живут в периодическом скринере (свежий код сам), storm-демоны шлют сырым _send.
- **vol_radar «Пробуждение» + ShortDistMute** (2026-07-05, решение брата по глубокому
  разрезу наших сигналов): (а) гига-спайк ≥15× на альте = класс «🌅 пробуждение»,
  доставляется ВНЕ капа 5/день и вне топ-1 (мини-кап 3/день с логом; ретро: 6ч-good
  0/6, ход приходит через 12-35ч — VANRY ×23.6 +52%/35ч был sent=0); чистые ядра
  select_awakenings/build_awakening_message, дедуп с одиночными карточками, юнит+
  selfcheck+dry-run пройдены. (б) short_dist в TG ЗАГЛУШЕН (месяц честного окна:
  n=509, WR 34%, avgR −0.15, половина потока алертов) — ранний return в
  _passes_setup_tg_filter с log_reject("ShortDistMute"); pending/resolved/контрфакты
  ЖИВУТ (save_pending берёт filtered ДО tg-фильтра — проверено); DistGate-цепочка
  сохранена под return для отката. Вечерний мьют 18-24 МСК брат НЕ одобрил — не делан.
- **vol_radar — анти-шум доставки** (2026-07-02): два направления алертов закреплены —
  vol_radar «заблаговременный» (спайк объёма при стоячей цене ≤1%) + storm IGNITE
  «в моменте» (первая минута пробоя); storm НЕ тронут. vol_radar слал 131/76/23
  сообщений в день → теперь: порог 4→5×, металлы (XAU/XAG/XAUT/PAXG) исключены как
  стоки, каскад ≥3 монет/скан = ОДИН дайджест «рыночный каскад» (кулдаун 2ч),
  одиночные = только сильнейший за скан, дневной кап 5 (UTC, radar_budget.json).
  Всё задетекченное пишется в radar_hits.csv (sent = факт доставки), кулдаун 4ч
  ставится всем детектам. Ретро-оценка пакета: ~2–7 сообщений/день. Селфчек:
  `python3 vol_radar.py --selfcheck`. **Дожим 02.07 (вечер): мажоры (MAJOR_SYMBOLS,
  28 имён) НЕ идут одиночными карточками** — резолв 245 хитов 29.06–02.07 по
  6ч-критерию брата: мажоры 3/80 хороших (3%) vs альты-одиночки 19/68 (27%);
  в каскад-дайджест и CSV мажоры пишутся как раньше.
- **radar_resolver — форвард-резолвер vol_radar-алертов** (2026-07-02, закрыл п.2
  аудита 01.07 «резолвера radar_hits.csv нет»): `radar_resolver.py`, зовётся из
  storm_report на каждом ежедневном тике + руками. **Критерий «хорошего» алерта
  предрегистрирован братом 02.07: окно 6ч, ход ≥5% в сторону большего движения
  при пиле против ≤1.5% (до пика) — менять только вперёд, с датой.** Пишет
  outcomes/radar_resolved.csv (append-only: dir/mfe/mae/t2_h/good + срезы
  major/cascade/sent), в шторм-отчёт добавляет секцию «🔊 Радар-карточки»
  (доля хороших: всего/доставленные/альты-одиночки/мажоры/каскадные, медиана хода).
  База на 02.07: хороших 37/245 (15%); сторона у хороших ~50/50 — направление
  по-прежнему за человеком. Селфчек: `python3 radar_resolver.py --selfcheck`.
- **storm_report — авто-отчёт отработки IGNITE-сигналов** (2026-07-02): `storm_report.py`,
  launchd `com.trading.stormreport` ежедневно 20:00 МСК, сам держит каденс «раз в 2 дня»
  (guard 44ч). Замер по 1м свечам Bybit, **без цели/стопа и WIN/LOSS (решение брата
  02.07 вечером: сторону не предсказываем — стопы не считаем)**: по каждой монете
  проценты в сторону пробоя — сейчас/итог, пик (MFE+когда), просадка (MAE), BTC за то
  же окно; отслеживание ≤7 дней → 🏁 снят с финальным итогом. Pending переносятся
  из отчёта в отчёт (`outcomes/storm_report_state.json`). Тот же скрипт вторым каденсом (guard 92ч) шлёт **«Сводку за 4 дня»**:
  по каждому поджигу пик за 24ч (цена/%/время), просадка, финал, детерминированный
  ярлык поведения и вердикт «можно ли было заходить» от реалистичного входа (close
  1-го полного 1м бара после алерта; «можно» = ≥2% хода при просадке до пика ≤1%).
  Стейт обновляется только после успешной доставки в TG (фейл → ретрай завтра),
  ключи отчёта и сводки мержатся, не затирая друг друга. Селфчек:
  `python3 storm_report.py --selfcheck` (16 проверок); превью: `--dry-run --force
  [--only report|summary]`.
- **Скринер** каждые 4ч (00/04/08/12/16/20 UTC) + строка `funnel: candidates= passed= rejected= top_reason=` в каждом прогоне (`6c18d87`)
- **Алерты с кнопками** [✅ Вошёл / ⏭ Пропустил] → `outcomes/alerts_index.json` → callback → `outcomes/trades.json` (trade_logger, status=open) → `/close SYM ±R|цена`, `/open` (`1ab24f7`…`7a0887f`)
- **A1 — петля замкнута для СКРИНЕРНОГО потока** (2026-06-08): топ-сетапы скринера уходят
  ОТДЕЛЬНЫМИ кнопочными алертами (`send_top_setup_alerts` → `send_signal_alert(verdict=)` в
  `telegram_alerts.py`), а НЕ бесконтактным текст-блоком (раньше кнопки были ТОЛЬКО на памп/раг,
  trades.json по скринеру = 0). Шлёт GO **и** macro-veto WAIT (как памп-путь). Уровни = Claude
  tp/sl — **показанное == записанное в trades.json == что сторожит trade_watcher**; направление =
  каноничный `r['setup_dir']` (НЕ эвристика по имени сетапа); стоп с полом **ATR×0.65** (guard от
  свипа, поверх флэт-клэмпа фильтра [1%..10%]). Фильтр зовётся ОДИН раз (`source=top_setups`,
  лимит 5/день per-источник), повторный Claude НЕ вызывается. **Дублей в батче нет** (текст-блок
  «ТОП СЕТАПОВ» удалён; watchlist/deep-dive — обзор по score, как были). Побочно почин лика
  бюджета: macro-veto WAIT по скринеру раньше помечался sendable (ел дневной лимит), но GO-only
  блок его НЕ доставлял → теперь маркировка == доставка. Живой тест end-to-end пройден
  (алерт в личку с кнопками → Вошёл → trades.json open → `/close +1R` → profitable → следы вычищены).
- **A2 — персистентный сток реджектов с контрфактами** (2026-06-08): `outcomes/rejected_history.csv`
  (append-only, upsert по ключу `минута|symbol|reject_gate`, контрфакты обновляются по мере
  дозревания) — `rejected.json` остаётся rolling-3000 (~5.6 дн), но история больше НЕ теряется.
  Врезка в `resolve_rejects` (reject_tracker.py) — зовётся скринером каждые 4ч, persist на той же
  точке (не отдельный процесс). `tools/gate_forensics.py` читает полный сток (`load_history`,
  приоритет истории + свежий live), форензика №2 идёт на полном окне, не на 5.6 днях.
  `backup_outcomes.py` бэкапит CSV еженедельно. Seed текущего окна выполнен (3000 строк, 02–08.06).
  Moves — СЫРЫЕ ценовые % (costing для реджектов нет). CLI: `python3 reject_tracker.py seed`.
- **A3 — KB авто-аналитика на ЧЕСТНОМ окне** (2026-06-08): `outcome_tracker.analyze_patterns` теперь
  считает ТОЛЬКО ≥2026-06-02 (история до неё НЕ перемигрирована — both-hit держит look-ahead,
  246/246 r=TP; живой 5m-first-touch резолвер честен с 02.06). Порог **n<25 → «недостаточно данных»**
  (убил ложь «range_sweep 58% n=12»); «лучший сетап» только из публикуемых; добавлен **net-R**
  (costed_r) рядом с сырым; шапка с линейкой/режимом/костами/датой. Окно ≈ Extreme Fear → числа
  РЕЖИМ-УСЛОВНЫ (не эджи, см. C1). Текущий KB: N=271, squeeze 55%/n=29 (режим), bos_fvg 42%/n=52,
  short_dist 32%/n=179 (НЕ 63% — то был пик-срез до краха 06.06), swing «мало данных». Перегенерён.
- **Macro-veto алерты**: враждебный макро при нормальном сетапе = WAIT с пометкой «⚠ МАКРО ПРОТИВ» в TG, решение за человеком (`7e6e21d`)
- **Obsidian fail-open** (`099d27d`); **Heartbeat** 0/6/12/18 — сирена в личку при тишине (`e47945e`)
- **Всё расписание на launchd, cron ПУСТ** (навёрстывание после сна): dailyreport 12:00 (+блок РЕАЛЬНЫЕ СДЕЛКИ), sectorheat */10, heartbeat ×4, rotatelogs вс 12:20, outcomesbackup вс 12:40 (`4f5acee`, `41f8621`); check_score_gate убран — one-shot, выстрелил 01.06
- **liqtracker**: тихие реконнекты (джиттер, сброс после 5 мин стабильности), лог ожил (мёртв с 14.04 — basicConfig без stream); retention 14 дней, WAL 25МБ→0 (`4d2d097`)
- **run_*.sh**: ранний cd + set -u — getcwd закрыт; все плисты с абсолютным WD (`4e981de`)
- **Маскировка токена** в логируемых TG-ошибках: `redact_token()` во всех 9 принтерах (P1-8c)
- **Вотчер открытых РЕАЛЬНЫХ сделок** (`trade_watcher.py`, демон `com.trading.tradewatcher`,
  замена signal_monitor): раз в 60с читает ТОЛЬКО `outcomes/trades.json` (status=open),
  при ≥1 сделке — ОДИН запрос `/v5/market/tickers?category=linear` на все символы, сверка
  last price с sl/tp1 по направлению (long: ≤sl/≥tp1, short: ≥sl/≤tp1) → пинг в личку
  (🔴 SL / 🟢 TP1, подсказка `/close SYM ±R`). Пусто → 0 сетевых вызовов. Анти-спам:
  событие (сделка×уровень) раз — отметка в сайд-файле `outcomes/watcher_state.json` под
  file_lock (НЕ вторая бухгалтерия, в trades.json не пишет); отметки закрытых сделок
  выпалываются. sl/tp=None → одна INFO-строка, не пинг.
  ⚠ **ОХВАТ (дисклеймер обязателен):** вотчер работает, только пока Мак не спит — это
  **ИНФОРМАЦИОННАЯ страховка, НЕ замена биржевого стопа**. Реальный SL всегда ставится
  на бирже; пинг — лишь напоминание «проверь/закрой», ложного чувства защиты быть не должно.
- **pump_detector — чистый выход на py3.14**: один резидентный event loop (`_arun`) вместо
  3× `asyncio.run()` на скан, SIGTERM → штатное завершение с await-закрытием aiohttp-сессии
  и loop ДО выхода (`_shutdown_loop`, try/finally в `__main__`). Шум `_ssock` AttributeError
  (×569 в error.log до 28.05) устранён корректным закрытием, без подавления. Проверено:
  ручной прогон 30с + SIGTERM → stderr 0 байт.
- **Персистентный общий cooldown Claude-фильтра** (F-61): `outcomes/claude_cooldown.json`
  под file_lock — вход `filter_candidate` первым делом отбивает дубль symbol|setup|direction
  (TTL пер-источник: pump 4ч / прочие 8ч — те же проектные числа) и дневной лимит
  5 send-able/день **ПЕР-ИСТОЧНИК** (решение владельца) ДО обращения к API; переживает
  рестарт. Отметка — на send-able (GO / macro-veto WAIT), как у старых in-memory механизмов
  (те остались pre-check'ами). Промоушен из wait_watchlist идёт МИМО precheck (trigger =
  подтверждение, не дубль — решение владельца). Известный минор: парный precheck/mark
  не атомарен между процессами (±1 к лимиту в гонке) — класс pre-existing, принято.

## 2. СЛОМАНО / МОНИТОРИМ

- **Воронку держат DistGate-C (~31.5% отказов) + ScoreGate (~26%) + DistGate-A (~18%) + HardGate**.
  Форензика №2 (к 10.06) — `tools/gate_forensics.py` теперь на ПОЛНОМ окне (`rejected_history.csv`,
  A2), не на rolling 5.6 дн. Текущий разрез (3000): DistGate-C 940 / ScoreGate 769 / DistGate-A 541 /
  HardGate 375 / DistGate-B 206
- **Claude-фильтр в макро-дедлоке** до выхода из Extreme Fear (0 GO с 03.06).
  След: `outcomes/shadow_verdicts.jsonl`, поле `macro_veto`
- **Диск ~82% full** (pump_analysis/ 532МБ — главный кандидат) → iCloud выгружает файлы
- **Токен-инцидент**: токен засвечен в локальных логах 06.06, владелец решил НЕ ротировать;
  логи вычищены до 0 вхождений, URL в логах маскируются с 2026-06-07; в git-историю не попадал

## 3. РЕШЕНИЯ ВЛАДЕЛЬЦА

- Система = **сканер для ручных решений**, не автосигналы
- Макро-вето → WAIT с пометкой (не SKIP); качественный мусор фильтр режет
- Реальный учёт — **только кнопки + /close** (никаких ручных правок trades.json)
- **signal_monitor выключен** (.disabled, код цел): дублировал главный конвейер ×24 чаще,
  второй реестр active_signals расходился с петлёй; −24× фоновых Bybit/Claude-вызовов
- **ngrok** .disabled — включить при нужде в удалённом дашборде
- Новые фичи были заморожены до конца P1 — **P1 закрыт**

## 4. НЕ ДЕЛАЕМ ОСОЗНАННО / БЭКЛОГ

- **C1 (08.06): short_dist/SHORT — НЕ боевой размер. Вердикт «закрыто» СТОИТ.** «+0.288 (n=233) GO»
  оказался кумулятивным ПИКОМ той же path-resolved метрики на обрезке 07.06 (Extreme-Fear 01–05.06,
  шорты +0.75…+0.89R/день), осыпавшимся до **−0.002 (n=306)** за день отскока 06.06 (n=66, −0.97R = сквиз).
  Регим-зависимая BTC-бета + optional stopping (правило сработало ещё на n=141/31.05). ×2-косты −0.019;
  n=306 = лишь 8 дней (cluster-CI ×6 шире); maker-филлы недоказаны. Разбор: `C1_shortdist_reconciliation.md`,
  `tools/c1_shortdist_stress.py`. Условия пересмотра — там же (форвард через смену режима + cluster-robust + филлы из trades.json).
- `USE_VALIDATED_V1=False` — ждёт честных форвард-данных (не in-sample)
  (факторы: archive/docs_2026H1/{PUMP,DUMP}_PATTERNS_ANALYSIS.md; комменты
  pump_detector.py:368 / rug_detector.py:104 ссылаются туда)
- Распил screener.py (357КБ) — после стабилизации; web_dashboard — не трогаем
- Мёртвые direction-гейты — осознанно (см. LIVE_CODE_AUDIT)
- **Бэклог P2**: ✅ вотчер открытых РЕАЛЬНЫХ сделок (`trade_watcher.py`, заменил
  signal_monitor — см. секцию 1); ✅ SL-кап проверен 07.06: претензия майского аудита
  («зажат 6%») неактуальна — кап уже 10% (`claude_realtime_filter.py:742`, фикс #8 от
  30.05, в git с 369a8ec), ATR-логика применяется ДО капа на обоих путях, механический
  план (`telegram_alerts.py:754/761`) вообще без капа — чистый ATR×0.65;
  осталось: форензика №2 гейтов; ✅ py3.14-краш закрыт 07.06 (clean shutdown);
  ✅ scanner V1 ПОГАШЕН и в архиве 07.06 (решение владельца: `com.trading.scannerdigest`
  слал TG-дайджест 3×/день — выгружен, plist+код+лог в `archive/scanner_v1/`;
  вернуть: bootstrap плиста из архива)

## 5. АРХИВ

**archive/docs_2026H1/ — история (25 доков трёх эпох).
Статусам и «DONE» там НЕ верить.** Живой статус — только этот файл.
