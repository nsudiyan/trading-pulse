# ── Секреты — никогда не коммитить ──
telegram_config.json
obsidian_config.json
channels_config.json
.env
.env.*
!.env.example

# ── Кэш / системное ──
__pycache__/
*.pyc
.DS_Store

# ── Логи / локи / pid — все ──
*.log
*.lock
*.pid

# ── Бэкапы кода (физический архив — отдельным этапом) ──
*.backup_*
*.bak
*.bak_*
*.bak.*

# ── Runtime-состояние и кэши ──
last_scan_cache.json
channel_signals_cache.json
active_signals.json
channel_accuracy.json
claude_rt_state.json
cooldown_cache.json
dynamic_blacklist.json
expansion_cooldown.json
pump_alert_state.json
.free_data_cache.json
.score_gate_check_done
.unlock_fetch_checkpoint.json
skills-lock.json

# ── Базы данных ──
*.db
*.db-shm
*.db-wal

# ── Runtime-каталоги (состояние, не код) ──
outcomes/
data/
charts/
tv_charts/

# ── Дашборд «Пульс»: стейты/данные — НЕ код (rose_history = посты платного
#    канала, в публичный снапшот нельзя; фид/леджер/кэши — runtime) ──
dashboard/rose_history.json
dashboard/signals_ledger.json
dashboard/channel_outcomes.json
dashboard/feed_push_state.json
dashboard/side_study_rows*.json
dashboard/site/feed.json
dashboard/logs/
dashboard/.vercel/

# ── Сгенерированные данные анализа (код .py и доки .md остаются в git) ──
dump_analysis/*.csv
dump_analysis/*.json
dump_analysis/plots/
pump_analysis/*.csv
pump_analysis/*.json
pump_analysis/plots/
pump_analysis/catalyst_data/
pump_analysis/klines_5m/
pump_analysis/klines_15m/
pump_analysis/klines_1h/
backtests/*.json
backtests/*.csv
calibration/*.json
calibration/*.png
calibration/*.md

# ── Архивы гигиены (P1-6): gzip-логи, файловые бэкапы, снапшоты данных ──
archive/logs/
archive/file_backups/
archive/outcomes_backups/

# ── Инструменты (Claude Code / Cursor / CodeGraph) ──
.codegraph/
.cursor/
.agents/
.claude/

# чужой проект, случайно в папке
aura-landing/

# скачанные рыночные данные TSMOM (re-fetchable, архивируем в перестройке)
research/daily/
research/daily_cache/
research/klines/
research/survivorship/
research/*.csv
carry/*.csv
calibration/.va_last_rowcount
backups/
archive/dead_2026-06-29/
dashboard/push_subs.json
dashboard/push_vapid.json
dashboard/combo_push_seen.json
dashboard/entry_logged.json
dashboard/diary.json
dashboard/rose_live_posts.json

# ── Large reproducible research outputs — source code stays tracked ──
backtest_6y/out/
flow_collector/server_archive/
flow_collector/data_dom/
local_structure_outcomes/
dashboard/site/dist/
dashboard/impulse_review_active.json
dashboard/impulse_acceptance_state.json
backtest_6y/*.txt
backtest_6y/*.json
flow_collector/parallel_start.txt

# Nested projects are backed up in their own repositories ──
hflow_replay/
xarb01/
xarb_obs02/
