# ПЛАН ПОСЛЕ БОЕВОГО ПЕРИОДА (готов заранее — брат, 2026-07-07)

> Старт: после вердиктов форвардов (~2026-07-20). До тех пор — ЗАМОРОЗКА.
> Порядок железный: ретро-валидация → observe 🧪 (дашборд, без TG) → боевое.
> У КАЖДОГО пункта есть критерий отказа. Пункт без валидации в код не идёт.

## ФАЗА 0 — Вердикты (день 1)

Собрать вердикт-отчёт по всем форвардам из STATE-таблицы:

| форвард | kill-критерий (зафиксирован заранее) |
|---|---|
| наклон v2/v3 | n≥25: точность <57% ИЛИ нижняя 95% CI <50% → правило удаляется |
| entry-воронка | 20 закрытых суток: распределение не лучше сырого пула → секция теряет статус зоны |
| связки | n≥15: преимущество ×2 не держится → блок сносится |
| 🎣 иглы | 2 нед: расхождение с ретро (43/61%) за CI → плашки убрать |
| погода BTC | месяц: 46/31 и 45/18 не воспроизводятся → плашки убрать |
| пробуждения ≥15× | n≥10-20: mfe-профиль vs ретро |
| выходы | повторный бэктест на ~2× данных: жив ли «держать хвост» |

Формат: outcomes/studies/verdicts_2026-07-20.md — по каждому: KEEP / KILL /
EXTEND(+2 нед) с цифрами. Убитое удаляется из кода в тот же день.

## ФАЗА 0.5 — Диспансеризация измерений (день 1–2)

- **Дневник: волновое загрязнение холодной базы.** Записи 06.07 включают
  волновые кандидаты (сервер тогда логировал волну, фронт скрывал) — при
  построении ожиданий классов пометить/исключить волновые дни из cold-base,
  пересобрать expectation-базы. Критерий: кластеры ≥5 записей одного 5-мин
  скана в diary.json.
- **Экзамен наклона: переякоренные треки.** bias_at_birth рождается при
  первом сигнале, а исход меряется от ПОСЛЕДНЕГО якоря (подтверждение ≤6ч
  сдвигает). Посчитать долю переякоренных среди graded и чувствительность
  точности к их исключению. Если сдвиг >5 п.п. — экзаменовать от якоря рождения.

## ФАЗА 1 — Ретро-валидации дыр (дни 2–4, только данные, ноль кода в бою)

### 1.1 «Волна №2» — ре-алерт по монетам со «звонками»
**Дыра:** задранная 7д-медиана объёма глушит повторный хит; цен-гейт ≤1%
режет разгон. Жертвы: VANRY (2×), BLUR, ZK (бар ×5.2 @ +0.99% в 09:20).
**Правило-кандидат:** монета с хитом ≥5× за последние 7д = «на взводе»:
для неё (а) база объёма = медиана без топ-10% баров, (б) цен-гейт ослаблен
до ≤2.5%/ч, (в) собственный кулдаун 2ч. Окно взвода 72ч от последнего звонка.
**Валидация:** ретро по radar_hits + klines за все недели: сколько ДОП.
алертов даёт правило, их mfe≥5% доля vs базовые ~30%, алертов/день.
**Принятие:** доп-good ≥25% И ≤3 доп-алерта/день. Иначе — не строить.
**Реализация:** vol_radar.py: armed_symbols() из radar_hits, ветка в
detect_spike; observe 🧪 неделю (класс «⚡ ре-алерт» только в зону+дневник).

### 1.2 Детектор дрейфа (одобрен братом 07.07)
**Дыра:** класс «+15–40%/24ч, объём растёт ВМЕСТЕ с ценой, без спайка ≥5×
и без вертикали ≥80%/4ч». Жертвы: TAC, BLUR, EVAA, ZK-утро, VANRY-волна2.
**Правило-кандидат:** скользящие 12ч: ret ≥ +8%; ≥60% зелёных 1ч-баров или
макс-ретрейс <40% хода; объём 24ч ≥ 2.5× суточной медианы 7д ПРИ max
5м-бара < 5× (иначе это спайк-класс, не дрейф); ΔOI 24ч ≥ 0; не мажор;
не в pump-эпизоде.
**Валидация:** ретро-скан 1ч-klines всех перпов за 5+ недель → все события
класса → форвард 24ч (mfe/mae/ret) vs случайная база.
**Принятие:** n≥30, mfe≥5% у ≥35%, вверх ≥60%. Пусто → похоронить с отчётом
(урок squeeze_radar).
**Реализация:** drift_radar.py в скане storm_radar (fail-open), класс
«дрейф» в зоне входа со СВОИМ ожиданием в дневнике (diary kind=drift,
холодный старт из ретро-валидации). TG не трогать до отдельного решения.

### 1.3 Когорта 4–5×
**Данные уже копятся** (radar_hits пишет с ~3×). Разрез: эпизоды
4≤ratio<5, дедуп 24ч, ≥3 недель: n, good, mfe≥5%, доля вверх vs 5×-когорта.
Улики ЗА: AERGO ×4.1 (+50%), SLX ×4.2 (+44%) — чемпионы недели ниже порога;
sensitivity-плато 3–5× (70/72/68%).
**Принятие:** n≥30 И метрики ≥ (5×-когорта − 5 п.п.) → порог зоны 5→4
(TG-порог обсудить отдельно: +18% сигналов = нагрузка на капы).
**Отказ:** метрики хуже → порог не трогаем, вопрос закрыт цифрами.

### 1.4 Цен-гейт «≤1%» — sensitivity
Прогнать гейт 1.0/1.5/2.0/2.5% на ретро: n сигналов и good-rate по каждому.
Если плато как у порога объёма — знаем запас; если качество падает от
ослабления — гейт остаётся, ослабление только внутри 1.1 для «взведённых».

## ФАЗА 2 — side_study-2 (дни 3–5, параллельно)

- **funding-лид вниз-пробоев**: n down-ignite удвоился → 17% vs 7% держится?
  Принятие: n≥60 суммарно, разница ≥8 п.п. → двухцветная плашка вниз-пробоев
  И — УГОВОР с братом (07.07 вечер): при подтверждении строится КРАСНАЯ
  ШОРТ-ЗОНА в тот же день (карточка/план/дневник-класс/форвард — все замки
  как у лонгов). live-счёт лида на момент уговора: 3/3 (MUSDT/ADA/MAGMA).
- **«Звонок → выстрел»**: формализовать паттерн DEXE/FIL/ZK — P(ход ≥10% в
  2–5 дней | был хит ≥5×) vs базовая P. Если ×2+ — это фича «взведённых» (1.1).
- **CVD-дивергенция**: поля cvd_kline/cvd_trade УЖЕ в resolved.csv — разрез
  по знаку CVD при сигнале (совет внешнего рецензента + давний план).
- **rose×пробуждение** n≥15: подтверждение ×2.
- **OI-раздутость анти-фильтр** (+5%/4ч перед сигналом): было n=3.
- **Вторые волны** (chg24≥10 на входе, метка уже копится): их mfe vs тихие.
- **Время суток / выходные** (пункт №3 от 06.07 — так и не сделан): разрез
  всех треков по будни/выходные и сессиям; ts есть везде, стоимость — час.
- **Расхождение парсеров Rose** (кейс LIT 46577, 07.07): превью-парсер
  (extract_signal) распознал пост, Telethon-парсер (parse_message) — нет →
  каноника не родилась, связка жила fast до 48ч. Унифицировать экстракторы
  ИЛИ canonical-фолбэк из маркера (маркер теперь несёт price поста).
- **🎣 sweep + OI**: ретро формулы был БЕЗ OI-подтверждения — если форвард
  плашек разойдётся с ретро (43/61), первым кандидатом добавить OI-чек
  (игла при |ΔOI|≈0 vs с ростом OI).

## ФАЗА 3 — Реализация выживших (дни 5–9)

Каждое правило, прошедшее валидацию: код → селфчек → observe 🧪 5–7 дней
(только дашборд+дневник со своим замороженным ожиданием) → сверка observe с
ретро → боевое включение (TG/пуши) только при совпадении. Расхождение
observe/ретро >CI → правило на доработку или в мусор.

## ФАЗА 4 — Деньги (только если ФАЗА 0 дала ≥2 KEEP)

Чек-лист micro-real: только сигналы зон · равный микро-сайз (риск на сделку
≤0.3–0.5% депозита; хвостовая логика: без тесного стопа, лимит на монету) ·
каждая сделка = запись дневника (план/факт) · стоп-условие месяца: PF<1.0
после ≥30 сделок ИЛИ DD>10% → пауза и разбор. Плечо минимальное. Никакого
усреднения. Выход — по правилам вердикта «выходов», не по настроению.
Execution-слой: в дневник поля real_fill/real_exit — с первого дня micro-real
меряем проскальзывание vs basis (klines-mid) и funding за холд; через 30
сделок — честный мост «статистика свечей ↔ карман».

## ФАЗА 5 — Гигиена и долги (фоном)

- pump_watch_block 3×/тик → 1× (кэш тика); унификация _fetch_5m_closed/sweep.
- Методологический отчёт для внешнего рецензента: таблица правило/n/WR/
  медиана/CI/p-value + OOS-протокол + риск-метрики бумажного этапа.
- Дневник v2: недельный авто-дайджест (Obsidian/пуш), кластеризация сюрпризов.
- Разобрать «слабый хит» класс для зоны (тихие ×4 без TG).
- push для pump-break/🎣 (опционально, по вкусу брата).
- PWA: авто-переподписка при visibilitychange (iOS изредка ротирует
  подписку; сейчас лечится только ручным 🔔).
- Кнопка «взял сделку» на карточке зоны → отметка в дневнике (мост между
  сигналами системы и реальными сделками брата) — обсудить с братом.

## Не делать (решено, чтобы не разъезжаться)

- Не добавлять новые ТГ-каналы (шум; Rose-пары хватает для связок).
- Не строить ML/скоринг уверенности до сотен размеченных сделок.
- Не расширять на MEXC/другие биржи до доказанного эджа на Bybit.
- Не менять методику 6ч/24ч (сравнимость с накопленным).
