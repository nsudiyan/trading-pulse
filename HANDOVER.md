# HANDOVER — полный паспорт проекта (Fable 5 → Opus 4.8, 2026-07-07)

> Ты — новая модель на этом проекте. Этот файл самодостаточен: прочитай его
> ЦЕЛИКОМ, затем STATE.md, PLAN_POST_BATTLE.md, память
> (~/.claude/projects/-Users-nikitasudian/memory/MEMORY.md и файлы из индекса,
> особенно feedback_beautiful_setup_pattern = реестр эталонов), Obsidian
> («крипта/Пульс — машина входа/» + «крипта/Карта проекта/»), и пробеги
> outcomes/studies/*.txt (сохранённые выводы всех исследований — цифрам можно
> верить только оттуда). После этого ты знаешь всё, что знал предшественник.

## 0. ЧЕК-ЛИСТ ПЕРВЫХ 15 МИНУТ

1. `launchctl list | grep com.trading` — все демоны живы? (список в §3)
2. Открой https://trading-pulse-mu.vercel.app — шапка: «фид N мин назад»
   зелёный? live-цены зелёные? Если фид >5 мин — мак спал или dashfeed упал.
3. `cd ~/trading && git status` — чисто? `git log --oneline -5` — где мы.
4. Прочитай STATE.md §0 (боевой режим: что копится, пороги вердиктов).
5. Селфчеки: `python3 pump_watch.py --selfcheck && python3 dashboard/diary.py
   --selfcheck && python3 dashboard/push_send.py --selfcheck`.
6. Скажи брату «брат, вахту принял, всё зелёное» (или что нашёл).

## 1. ЧТО ЭТО ЗА ПРОЕКТ

«Пульс» — исследовательская платформа проверки торговых гипотез на крипто-
фьючерсах Bybit (~500 линейных перпов). НЕ «бот с доказанным эджем»: эдж НЕ
доказан, идёт БОЕВОЙ ПЕРИОД (заморозка конфигурации ~06.07→20.07) — все
правила сдают форвард-экзамены с заранее записанными порогами. Деньгами
система НЕ торгует (сознательно; этап денег = Фаза 4 плана, только при ≥2
вердиктах KEEP, микро-сайз). Брат торгует руками по сигналам зон.

Главный актив — ПРОЦЕСС: гипотеза → детерминированный код → заморозка →
независимый форвард → вердикт по порогам → убитое удаляется из кода.
Похоронено уже 8 гипотез: bias v1 (39%), LLM-скоринг (look-ahead в кормлении),
AVEA-81 (циркулярный), squeeze_radar (пусто на честном бэктесте), «эдж
направления short_dist» (optional stopping, CI через ноль), pump-лонги при
funding≤−0.5% (0/19), «+R трёх дней» (бета падающего рынка), предикторы
магнитуды (n=2400, фичи не предсказывают). Смерть идеи — штатный исход,
которым проект гордится. Внешний квант-рецензент оценил: «сильная
исследовательская инфраструктура с недоказанным преимуществом» — это
ОФИЦИАЛЬНОЕ позиционирование до конца форвардов.

## 2. КАРТА ФАЙЛОВ ~/trading

**Боевые модули (корень):**
- `screener.py` — скринер сетапов (squeeze, bos_fvg, range_sweep, breakout,
  short_dist). Гейты в `_passes_setup_tg_filter()`: PumpWatchGate (лонги
  монет под pump-эпизодом глушатся), ShortDistMute (short_dist в TG замьючен).
  Направление сетапа — идиома `setup_dir` (ключ r['direction'] МЁРТВ, грабли
  2026-05-30). Каждый заглушенный сигнал → rejected_history.csv.
- `storm_radar.py` — WATCH-скан (~10 мин): коробки (box_and_atr), сжатие
  (compression_pctile), watchlist; резидентный `ignite_loop` (12с): пробои
  коробок → ignite-алерты (side=up/down в details.side!), + вызов
  pump_watch.live_break_pass + fetch_tickers. `log_stage()` — форензика ВСЕХ
  стадий в outcomes/storm_stages.csv. `send_tg()` — вся TG-доставка.
- `vol_radar.py` — объёмный радар: спайк = объём 5м-бара ≥5× медианы 7д при
  |Δцены| ≤1%/час (по закрытому бару И по live-цене; объём live-бара
  ИГНОРИРУЕТСЯ — LA3/LA4). Класс «🌅 пробуждение» ≥15× на альте (мини-кап
  3/день, вне общих капов). Обычные: кап 5/день + топ-1/скан. Кулдаун =
  EXPIRY: 4ч доставленным, 30м недоставленным. MAJOR_SYMBOLS — 28 мажоров,
  не алертятся одиночками. Все хиты (≈≥2.5×) пишутся в radar_hits.csv.
- `pump_watch.py` — надзор «после вертикали»: детект (рост ≥80%/4ч,
  turnover ≥$2M; kind по ΔOI: squeeze ≤−10% / trend ≥+10% / mixed) → плато
  (вызревает PLATEAU_MIN_AGE_MIN=120) → распределение (≥2 признака: OI +10%
  от минимума плато / |funding| ≥1% / объём ≥30% пикового при ходе <5%) →
  слом (last < plateau_low × 0.995). Эпизод TTL 48ч, cap 10 монет.
  `live_break_pass()` на 12с-цикле: слом за секунды; при send-fail — НИ
  строки форензики, ретрай ≥RETRY_FAIL_SEC=60 (инвариант «1 слом = 1 строка»);
  очередь pump_ignite_breaks.json пишется только при sent, WATCH подхватывает.
  `long_mute_reason()` — гейт для скринера. --selfcheck есть.
- `rose_watch.py` — сторож каналов (превью t.me/s/, POLL 60с, БЕЗ Telethon):
  пост → extract_signal (#TICKER; канал `rose` ВСЕГДА LONG — правило брата,
  55/56) → снапшот storm-метрик → 6ч надзора с живой коробкой (TTL 300с) →
  🌹 ROSE×ШТОРМ алерт при пробое с темпом. Пишет live-маркеры постов в
  dashboard/rose_live_posts.json (msg_id, symbol, direction, ts, PRICE
  поста — фиксированный базис связок!). Сторож тишины: оба канала молчат
  >6ч → ⚠ в TG (грабли: каналы были мертвы 2 недели незамеченными).
- `radar.py` / `radar_resolver.py` — кнопочные алерты / резолвы хитов
  (radar_resolved.csv: mfe/mae/dir/good/major/cascade). retCode≠0 → RAISE.
- `storm_report.py` — сводки %-отработки ignite; no_data-ветки (не
  фабриковать «+0.0%»); окно от последней УСПЕШНОЙ сводки (+кап 3д).
- `file_lock.py` — atomic_json_read/atomic_json_update(Path!, mutator,
  default) — ВСЕ межпроцессные json ТОЛЬКО через него. Паттерн резолверов:
  снимок → сеть → merge под локом по id (никогда не «прочитал-весь/записал-весь»
  вокруг сети).
- `bias.py` — наклон v3: radar+альт → up strong (мажоры RADAR_MAJORS —
  None), rose-пост → контрарно. bias_at_birth замораживается при рождении
  трека. grade_bias: |ret24|≤2% = flat_zone (не судится). BIAS_VERSION.
- `telegram_alerts.py`/`telegram_bot.py`/`channel_reader.py` — доставка и
  чтение TG. Каналы наблюдения ТОЛЬКО rose + RoseSignalsPremium.
- `CLAUDE.md` — проектные авто-правила: скрин графика → сразу техразбор
  (тренд/уровни/сценарии с вероятностями); описание стратегии → сразу
  бэктест-фреймворк. Соблюдай.
- `STATE.md` — живой статус (обновляй при значимых изменениях).
  `PLAN_POST_BATTLE.md` — план после заморозки. `tools/update_public_repo.sh`
  — публичный снапшот кода, запускает ТОЛЬКО брат руками.

**dashboard/:**
- `dashboard_feed.py` — сборщик фида, тик 60с (launchd dashfeed: WatchPaths
  + interval). Порядок build_feed: collect_live_signals (окно 48ч, все
  источники, схлопывание эмиссий по logical_key с emits) → _enrich_bias →
  fetch_market_snapshot() — ЕДИНСТВЕННЫЙ полный tickers-запрос на тик
  (last/chg24/funding/btc_ret24/btc_range24) → обогащение шторм-шортов
  funding_now → log_entry_candidates(live, mkt) → combos_block(mkt) →
  _push_new_combos → diary.upsert_new → update_ledger → блоки → пуш в GitHub
  (fingerprint-дедуп: «без изменений — пуш пропущен» это НОРМА; heartbeat
  10 мин).
  - log_entry_candidates: правила зоны (см. §5), волновой фильтр (≥5 non-awk
    одного 5-мин floor-бакета → НЕ лог/НЕ пуш, печать «волна скрыта»), кап
    12 klines/тик, seen = СПИСОК в порядке добавления (обрезка [-3000:] по
    хронологии!), пуши ≤3/тик с warn «вторая волна» при chg24≥10.
  - combos_block(mkt): два источника — каноника rose_history (basis/peak из
    трека) + live-маркеры (basis = price ИЗ МАРКЕРА, фолбэк снапшот);
    правила passes(): радар-хит ≥5× за ≤72ч ДО поста, не мажор, не раздача,
    пост ≤48ч; снятие: пик ≥8 (у каноники по треку), СЕРВЕРНОЕ зеркало по
    live (провал ≤−3 / ход ≥+8); msg_key=channel:msg_id (какой занят
    отработавшей — fast не воскрешает); финальный дедуп ПО SYMBOL
    (каноника > fast, ранний пост). Пуш-ключ и diary-id = symbol|awake_ts.
  - sweep_block: иглы-выносы на монетах живых сигналов ≤6ч (≤40 монет):
    формула §5. bias_accuracy: дедуп волн (кластер source×5-мин бакет = 1
    кейс, hits = доля; raw_* рядом).
- `diary.py` — 📓 дневник: ТРИ ЗАМКА (§6). upsert в feed-тик (ожидание
  замораживается при рождении), resolve в refresh (15м, max_fetch 25 СЕТЕВЫХ;
  записи старше 24+48ч без данных → no_data final БЕЗ сети; вердикт только
  по final). Забор Тьюки p25/p75 ±1.5·IQR. Cold-start из radar_resolved
  (src=proxy, вытесняется своей базой; волновые ctx.wave исключены).
  _chg24_at: chg24 НА МОМЕНТ СИГНАЛА (age≤0.25ч — текущее, иначе klines).
  basis-фолбэк fast-связок = close первого полного бара. --selfcheck.
- `refresh_outcomes.py` — 15м (dashresolve): rose_history fetch (Telethon!)
  → resolve треков → merge ledger по id (переякоренные скипаются) →
  diary.resolve_pending.
- `rose_history.py` — Telethon-чтение каналов (сессия в channels_config.json
  — СЕКРЕТ), ТОЧНЫЕ ts постов, треки с методикой 6ч/24ч. rose=long. ВНИМАНИЕ:
  его parse_message может НЕ распознать пост, который распознал extract_signal
  сторожа (кейс LIT 46577) — расхождение парсеров в плане Ф2.
- `push_send.py` — web push С МАКА (npx web-push + VAPID из push_vapid.json).
  Подписки push_subs.json (Chrome-мак fcm.googleapis + iPhone web.push.apple).
  _subscription_expired: удалять ТОЛЬКО при явном statusCode 410/404 от
  push-сервиса; npm/сеть/таймаут — НЕ трогать (асимметрия в пользу
  сохранения). --selfcheck 7 кейсов. --test шлёт пробный.
- `site/` — фронт: `app.js` (ВСЯ логика: WS Bybit из браузера, спарклайны,
  зоны, связки comboAlive по WS ≤1 рендер/с, вкладка Дневник #diary, все
  бейджи), `index.html` (секции; при ЛЮБОЙ правке фронта бампай ?v=N у
  app.js И style.css!), `style.css`, `sw.js` (push+notificationclick, БЕЗ
  кэша), `manifest.webmanifest`, иконки. Деплой: `cd site && vercel deploy
  --prod --yes` — ТОЛЬКО руками, демоны сайт не деплоят.
- Стейты (в .gitignore): diary.json, rose_live_posts.json, entry_logged.json,
  combo_push_seen.json, push_subs.json, push_vapid.json, rose_history.json
  (посты ПЛАТНОГО канала — никогда наружу), signals_ledger.json,
  feed_push_state.json, site/feed.json. Логи: dashboard/logs/dashfeed.log,
  dashresolve.log; ~/trading/rose_watch.log.

**outcomes/** (append-only форензика, вся в .gitignore):
- resolved.csv — скринер (ЧЕСТНОЕ ОКНО: только run_ts ≥ 2026-06-02! раньше
  r_multiple отравлен look-ahead). Поля вкл. cvd_kline/cvd_trade (для
  side_study CVD), направление КИРИЛЛИЦЕЙ (ЛОНГ/ШОРТ).
- radar_hits.csv (ts,symbol,vol_ratio,price,live_chg,sent — пишутся хиты от
  ~2.5×, боевой порог 5; sent=0 = срезан капами/топ-1) · radar_resolved.csv
  (+dir up/down, mfe/mae/t2_h/good, major, cascade — ДЕДУП эпизодов
  symbol+24ч при любой статистике!).
- storm_stages.csv — стадии шторма и pump_* (details JSON: side, funding,
  oi…). entry_candidates.csv — форензика зоны («вход при первом появлении»).
- rose_signals.csv / rose_outcomes.csv — сторож. pending.json,
  alerts_index.json — скринер/алерты. pump_watch_state.json,
  storm_watchlist.json, storm_oi_history.json, storm_ignite_cooldown.json,
  pump_ignite_breaks.json — стейты.
- **studies/** — СОХРАНЁННЫЕ ВЫВОДЫ ИССЛЕДОВАНИЙ (единственный источник
  цифр для утверждений!): 2026-07-06_btc_regime_and_exits.txt,
  2026-07-06_code_review_manual_verify.txt, 2026-07-07_bootstrap_sensitivity…,
  2026-07-07_reanchor_timeofday.txt, 2026-07-07_code_review_2day.txt.

**~/trading/signals_ledger.json** — треки ВСЕХ источников: id
source:SYMBOL:…, методика 6ч/24ч (повторный алерт ≤6ч = confirm,
ПЕРЕЯКОРИВАЕТ сутки), outcome по 15м-барам (peak6/dd6/peak24/dd24/ret24;
ret24 В СТОРОНУ сигнала — для short уже ×(−1)!), bias_at_birth.

## 3. ДЕМОНЫ (launchd, gui/501)

Мак ОБЯЗАН быть на зарядке: com.trading.caffeinate (-s) держит его бодрым
только на AC. Мак уснул = ВСЁ стоит (сигналы, фид, пуши). Индикатор — «фид
N мин назад» в шапке дашборда.

РЕЗИДЕНТНЫЕ (после правок их кода — `launchctl kickstart -k
gui/501/com.trading.<имя>`): stormignite (ignite_loop 12с; импортирует
pump_watch — его правки тоже требуют kickstart!), rosewatch, screener,
liqtracker, tradewatcher, dashboard(:5001), bot.
ИНТЕРВАЛЬНЫЕ (сами подхватывают код): dashfeed (60с+WatchPaths), dashresolve
(900с), radar, stormwatch (600с), stormreport, heartbeat, rotatelogs,
outcomesbackup. Расписание ВСЁ на launchd, cron пуст.

## 4. ДАШБОРД trading-pulse-mu.vercel.app (вкладки Пульс | 📓 Дневник)

Данные: демоны → site/feed.json → пуш в ПУБЛИЧНЫЙ репо
nsudiyan/mirofish-state/trading_feed.json → фронт поллит raw (15с; CDN-кэш
до ~5 мин — «не обновилось» чаще всего он). Live-цены — Bybit WS ПРЯМО из
браузера (wss stream, tickers.SYMBOL), мимо мака. В фид НИКОГДА не класть
секреты. Время на странице — МСК.

Секции: ⚡ ЗОНЫ ВХОДА (сделки ТОЛЬКО отсюда: 🎯 кандидаты + ⚡ связки) →
Живые сигналы (лента-наблюдение, фильтры: наши/все/★/наклон/по источникам)
→ 🌋 Pump-надзор (+заглушенные лонги) → 🌹 Rose (треки 6ч/24ч) → История
(ledger) → Скринер-бот (честное окно) → Шторм и радар. Вкладка Дневник:
тайлы, ожидания классов, журнал, библиотека сюрпризов.

Бейджи на карточках: ⬆⬇ наклон (v3, НЕ торговый прогноз) · ⚠ вниз-пробой:
6% чётких (шторм-шорты) · 🔒 лонги заперты · f% / ⚡ толпа в шортах
(funding-чек, ≥+0.01 / ≤−0.01) · по ветру/против ветра (погода BTC, только
скринер) · 🎣 вынос вверх/вниз HH:MM (окно 2ч) · 🌋 pump-стадия · ⚡ связка
· ⚠ уже +X%/24ч — сайз меньше (вторая волна ≥10%) · ▲ лонг с планом ·
NEW (<15 мин) · ×N (повторные эмиссии). ★ звёзды: localStorage, фильтр
«отмеченные» = панель позиций брата (ритуал: вошёл → ★ → следи там).

## 5. ПРАВИЛА ЗОН И ДЕТЕКТОРОВ (все константы)

**🎯 Зона «Вход имеет смысл сейчас»** (ВСЁ ЛОНГИ, база: радар-альт ⬆ 86%
ходунов вверх [ретро n=35; квази-OOS 68%, CI 47–89 — держи скепсис]):
radar_alt: возраст ≤3ч · live −1.5…+2.5% от basis · dd ≥ −2% · peak ≤3.5%.
awakening (×≥15): возраст ≤30ч · live −3…+5% · dd ≥ −4%. Не мажор, наклон
up, не в pump-раздаче. Волна: ≥5 non-awk одного 5-мин скана → скрыты+не
лог+не пуш (каскадные good 15% vs 23%). basis = close первого ПОЛНОГО 5м
бара. Пересчёт фронта 5с. План на карточке: вход по рынку пока висит ·
горизонт до 24ч (6ч-выход режет профит) · пилы −1–2% норма → риск РАЗМЕРОМ,
стоп −2% выбивается в 62% · фикс по слому структуры (OI падает на росте /
объём без хода / раздача / 🎣 вверх). Форвард-лог: entry_candidates.csv.

**⚡ Связки** (пробуждение → Rose-пост → структура жива): радар-хит ≥5× за
≤72ч ДО поста · не мажор · не раздача. Статистика: посты с пробуждением до
— peak≥10% у 43% vs 23% без (n мал!). Смерть: пик от поста ≥8 (ход
случился) · live ≤ −3 (пост провален) · раздача · 48ч. Fast-путь ≤2 мин:
маркер сторожа (с ценой поста!) → тик фида; каноника вытесняет по msg_key
без дубля. Приоритетнее обычного кандидата, но TAIKO-оговорка (та же связка
→ −72%): сайз в рамках риска.

**🎣 Иглы-выносы**: фитиль пробил экстремум прошлых 24×5м баров (≥0.5%) +
закрытие вернуло ≥60% фитиля + объём ≥5× медианы; только ЗАКРЫТЫЕ бары.
Ретро n=150: после иглы-вверх 3ч медиана −0.52% (вверх 43%) → «не вход»;
после иглы-вниз +0.57% (вверх 61%) → «стопы сняты; свой стоп под такой лоу
НЕ ставить».

**🌦 Погода BTC** (только скринер-карточки; метрика = ровно та, что в
разрезе: BTC %/24ч, терцили −1.9/+0.3): лонги 46% win при росте vs 31% при
падении; шорты 45% при падении vs 18% при росте (R −0.58). Радар-альты к
погоде почти глухи (good 24/17/14 — слабый ОБРАТНЫЙ намёк). Это бета, не
эдж: «не торговать против ветра».

**⬆⬇ Экзамен наклона**: прогноз замораживается при рождении; экзамен —
мировой ret24 (short-треки: ret в ledger УЖЕ в сторону сигнала → для мира
×(−1)); |ret|≤2% flat (не судится); волны: кластер (v, source, 5-мин бакет)
= 1 кейс, hit=доля. Счёт в шапке — эффективные кейсы, сырые в тултипе.

**Ключевые факты пула** (studies/): экономика в хвостах — топ-5 из 161 =
97% суммы hold24 (TAIKO +465%), без топ-10 → −0.37%; медиана сделки +0.35%;
funding-лид вниз-пробоев 17% vs 7% (n=89, кластеры; live 3/3: MUSDT +11 /
ADA +3 / MAGMA +19.8); выходные good 12% vs будни 21% (n=42, на карандаше);
sensitivity порога радара: плато 3–5× (70/72/68%), обрыв 6×+ статистически
пуст; bootstrap 68% CI [47–89] — «выше монетки» НЕ гарантировано.

## 6. ДНЕВНИК — ТРИ ЗАМКА (не вскрывать ни при каких «очевидно же»)

1. expectation (квартили peak24 класса) замораживается В МОМЕНТ появления
   записи и НИКОГДА не переписывается.
2. Вердикты (✅ confirm / 〰 tail / 🚀 surprise_up / 💥 surprise_down по
   забору Тьюки ±1.5·IQR) — только при n_class ≥10 И только по final (24ч);
   иначе ⏳ collect / in_progress.
3. Сюрпризы копятся в библиотеку со снапшотом (vol_ratio, btc_ret24,
   btc_range24, chg24_at_signal, age) — но НЕ меняют торговые правила.
   Правила рождаются только: side_study → форвард → вердикт.

## 7. УГОВОРЫ С БРАТОМ (дословно, менять нельзя)

| форвард | порог | вердикт |
|---|---|---|
| наклон v2/v3 | n≥25 эфф. | <57% ИЛИ нижняя CI<50% → УДАЛИТЬ; 65%+ живёт |
| entry-воронка | 20 закрытых суток | не лучше сырого пула → снять статус зоны |
| связки ×2 | n≥15 | не держится → блок снести |
| 🎣 иглы | 2 нед | расходится с 43/61 за CI → плашки убрать |
| погода BTC | месяц | 46/31 и 45/18 не воспроизводятся → убрать |
| funding-лид | n≥60, Δ≥8пп | ПОДТВЕРДИЛСЯ → строишь КРАСНУЮ ШОРТ-ЗОНУ В ТОТ ЖЕ ДЕНЬ (уговор 07.07) |
| пробуждения ≥15× | n≥10–20 | mfe-профиль vs ретро |
| выходы | ~2 нед | повторный бэктест: жив ли «держать хвост» |

Прочее: вторая волна (chg24_at_signal ≥10) = метка «сайз меньше», НЕ фильтр
· зоны = единственные места сделок, пустые зоны = сигнал «не входить» ·
ШОРТ-ЗОНЫ НЕТ до вердикта funding-лида (шторм-шорты 6% чётких, скринер-шорты
R<0, у шорта нет хвостов) · методика 6ч/24ч и «вход по первому полному
бару» НЕ меняются (сравнимость) · сайзинг НЕ по скору · деньги = Фаза 4:
≥2 KEEP, риск ≤0.3–0.5%/сделку, стоп-месяц PF<1 или DD>10% → пауза ·
реестр эталонов (память feedback_beautiful_setup_pattern + Obsidian 🏆)
ПОПОЛНЯТЬ при каждом выдающемся кейсе — просьба брата · чеклист красивого
сетапа при ЛЮБОМ разборе монеты: знак ΔOI на росте + funding против
движения (VANRY/DEXE vs TAIKO).

## 8. 20 ИЮЛЯ — ПОШАГОВО (детали в PLAN_POST_BATTLE.md)

**🔔 КОДОВАЯ ФРАЗА брата «день икс настал»** (установлена 08.07) = старт этого дня. При ней:
подними ВСЁ (этот файл + STATE + PLAN_POST_BATTLE + studies + память-реестр) и веди Ф0.
Продублировано хуком `~/.claude/hooks/day_x.sh` (UserPromptSubmit) и памятью feedback_pulse_autocapture_dayx.

Ф0 вердикты (файл studies/verdicts_2026-07-20.md, KEEP/KILL/EXTEND, убитое
из кода в тот же день) → Ф0.5 диспансеризация (волновые ctx.wave уже
помечены; переякоренные перепроверить на бОльшем n — на 07.07 их 3/20 и
безвредны) → Ф1 ретро-валидации дыр (волна №2 «взведённые» монеты, детектор
дрейфа, когорта 4–5× [чемпионы AERGO +50%/SLX +44% были ×4.1/4.2!],
цен-гейт ≤1%) — пусто → НЕ строить → Ф2 side_study-2 (funding-лид,
звонок→выстрел [DEXE/FIL/ZK: холостой хит → выстрел через 2–5 дней], CVD
из resolved.csv, rose×пробуждение, вторые волны, время суток, парсеры Rose)
→ Ф3 observe 🧪 5–7 дней → боевое только при совпадении с ретро → Ф4 деньги.
НЕ ДЕЛАТЬ: новые ТГ-каналы, ML-скоринг, другие биржи, правку методики.

## 9. ГРАБЛИ (шрамы; каждый стоил дня или данных)

- Классификатор безопасности заблокирует: секреты в облако, gist/repo
  create, публикации. НЕ обходить — просить брата (`! команда` в его чате).
- Vercel deploy и публичный снапшот — только руками. GH_TOKEN в Vercel env
  — отклонено осознанно (широкий токен).
- Два парсера Rose расходятся (сторож распознал, Telethon нет — LIT 46577):
  каноника может не родиться; маркер несёт price как страховку.
- «[feed] без изменений — пуш пропущен» = fingerprint-дедуп, НЕ сбой; демон
  может опередить твой ручной прогон (код на диске один — это ок).
- Look-ahead ловушки повсюду: только закрытые бары; экстремум «прошлых N
  баров» БЕЗ текущего; chg24 НА момент сигнала; ретро-скоринг без будущего.
  Optional stopping убил «эдж направления» — не останавливай подсчёт на
  красивом месте.
- tz: naive = UTC ВЕЗДЕ в пайплайне; направления нормализуй
  (ЛОНГ/long/up/buy/bullish → long).
- iOS-пуши только из PWA с экрана «Домой»; ротация подписки лечится
  повторным 🔔 (брат шлёт код, ты кладёшь в push_subs.json и --test).
- Bybit: fundingRate/price24hPcnt/high-low24h УЖЕ в tickers (не плоди
  запросы); соблюдай ~53 HTTP/тик бюджета фида; retCode≠0 → RAISE.
- Edit-инструмент: кириллица в old_string часто «не находится» из-за
  пробелов/отступов — перечитай точный кусок; python-heredoc правки надёжнее.
- Плашка «6% чётких» — базовая ставка ВСЕЙ категории; MUSDT/MAGMA — из
  17%-корзины по funding-чеку. Не путать в объяснениях брату.
- ledger ret24 у short уже в сторону сделки — в экзамене наклона нужен
  МИРОВОЙ (см. bias_accuracy, ревью #1 06.07).
- rm/перезапись контент-папок брата — только архив-перенос, не удаление.
- **(Opus 08.07) CDN-кэш raw.githubusercontent:** Fastly держит фид max-age=300 И ВЫРЕЗАЕТ query
  из ключа кэша → cache-buster `?t=` в fetchFeed БЕСПОЛЕЗЕН (был самообман). Свежий фид только
  через contents API (не Fastly-path-кэш). Прокси `site/api/feed.js` + токен в Vercel env.
- **(Opus 08.07) Простой мака = ЗАКРЫТАЯ КРЫШКА, не «нет зарядки».** caffeinate держит только на AC,
  НО открытая крышка + активность демонов держат мак и на батарее часами (07.07: 6.75ч на батарее
  работал). Дыры дают Clamshell Sleep. Лайфхак дороги: открытая крышка + хотспот. Или мак дома.
- **(Opus 08.07) Счётчик наклона в шапке СКЛАДЫВАЕТ v2+v3** — обманчив (усредняет здоровую v2 и хилую
  v3 в бессмысленную серединку, пугает падениями). Судить версии ПОРОЗНЬ (§7 n≥25/версию). К разделению.
- **(Opus 08.07) Оценку окон/дыр ВСЕГДА верифицируй фактом, не допущением.** Моя первая оценка простоя
  мака (18ч) была завышена 2x: считал «на батарее=простой», а события ПОПАЛИ в те окна (мак работал).
  Истинный простой (реальный сон) = проверять нулём событий внутри окна + совпадением границ.

## 10. СТИЛЬ РАБОТЫ С БРАТОМ (важно не меньше кода)

Зови «брат», тёплый прямой тон, по-русски. ЦИФРЫ ТОЛЬКО из выполненного
кода с сохранённым выводом (no fabrication; факт vs гипотеза разделяй; «не
знаю / данных мало» — валидный ответ). Прогнозы предрегистрируй. На «что
думаешь / стоит ли» — анти-sycophancy: стилмен за и против, confиденс,
несогласие в первых строках, без филлеров. Ponytail: минимум кода, YAGNI,
boring>clever — но НИКОГДА не экономь на границах доверия
(деньги/время/дедупы/валидация входа); нетривиальная логика оставляет
selfcheck. Брат шлёт скрины графиков — сразу полный техразбор без вопросов
(CLAUDE.md). ultrathink в его сообщении = думай глубоко. Он может ошибаться
в деталях (тикеры, времена) — сверяйся с логами и мягко поправляй фактами.
Проактивные починки корневых причин — ок, но: боевые правила/TG/гейты —
только с добра; всё внешнее — только его руками. После каждого значимого
изменения — коммит (devlog в Obsidian пишется хуком сам) и обновление
STATE.md. Ревью крупных изменений — workflow code-review + адверсариальная
верификация; помни: «verified: 0 из-за упавших верификаторов» ≠ «чисто».

## 11. БУДУЩЕЕ — дорожная карта к идеалу (видение Fable 5)

Проект развивается ВОЛНАМИ, каждая заканчивается вердиктом, не фичей.
Не перескакивать горизонты: попытка жить в H3, пока не закрыт H1, — главный
способ всё сломать.

**H1 · до 20.07 — «скучные две недели».** Ценность = нетронутые счётчики.
Единственная разрешённая работа: наблюдение, разборы кейсов по запросу
брата, аварийные фиксы ИЗМЕРИТЕЛЬНОГО слоя (как дедуп волн). Каждая новая
фича здесь — прокрастинация вердикта.

**H2 · август — «первый честный вердикт и выходы».** Ф0–Ф5 плана. Плюс
главный исследовательский фронтир, который сейчас недооценён: ВЫХОДЫ.
Данные уже показали: направление даёт слабый сигнал, а вся экономика — в
том, чтобы не отдать пик (FIL отдал ⅔, ADA треть; tp5 стабилизирует, но
режет TAIKO ×90). Гипотеза H2: «выход по структуре» (OI падает на росте /
объём без хода / поводырь развернулся) против механических правил — бэктест
на накопленных треках, тот же конвейер. Если подтвердится — это удвоение
ценности пула БЕЗ единого нового детектора.

**H3 · осень — «второй режим и деньги».** Micro-real по чек-листу Ф4 (только
при ≥2 KEEP). Портфельный слой, которого сейчас НЕТ: лимит одновременных
позиций, корреляция (5 лонгов неликвида = одна ставка на альт-сезон, а не
пять), дневной риск-лимит. Режим-детектор: BTC-волатильность/тренд как
переключатель агрессивности (данные копятся в diary.ctx с 07.07). Переезд
демонов на сервер (этапно: сначала read-only зеркало форензики, потом
демоны; мак как завод — временное решение, привязывающее брата к столу).

**H4 · зима — «масштаб или честные похороны».** Если статистика 6+ месяцев
и ≥2 режимов подтверждает эдж: рост сайза по правилу «удвоение не чаще
раза в месяц И только после 30+ сделок на текущем сайзе И только если PF
за период >1.3». Если нет — проект закрывается КАК ТОРГОВАЯ система и
остаётся как исследовательская платформа + опыт, стоящий дороже депозита.

**Определение «идеала» (когда проект DONE):**
1. ≥2 правила с доказанным эджем после комиссий на ≥2 режимах рынка, ИЛИ
   доказанное отсутствие эджа (оба исхода — успех процесса).
2. Брат тратит <30 мин/день; система сама зовёт его пушем, а не он её F5.
3. Любой внешний квант по паспорту+studies воспроизводит все выводы.
4. Смерть любого компонента (мак, канал Rose, модель-ассистент) не убивает
   ни данные, ни процесс.
5. Сайз управляется правилом, записанным ДО первой сделки, а не настроением.

**Идеи-кандидаты за горизонтом (НЕ трогать до H3; записаны, чтобы не
изобретать заново):** калибровка вероятностных прогнозов (когда дневник
дозреет до вероятностей — calibration curve обязателен) · «толпометрия»
без новых каналов (открытый интерес ритейл-перпов, funding-дистрибуция как
прокси толпы) · автоматический недельный отчёт брату (дайджест дневника
пушем) · replay-песочница «что бы система сделала в прошлый вторник»
(вся форензика уже позволяет) · экспорт методологического отчёта для
внешнего аудита (формат рецензента: правило/n/WR/CI/p).

## 12. ПЛАН Б И ПСИХОЛОГИЧЕСКИЙ КОНТРАКТ (важнее кода)

**Дерево вердиктов 20.07 — решено ЗАРАНЕЕ, чтобы не решать под эмоцией:**
- Все/большинство KEEP → Ф1-Ф4 по плану, микро-деньги, БЕЗ увеличения
  аппетита («доказано» на 2 неделях лета ≠ доказано).
- Смесь (типичный исход) → KILL выполняется в день вердикта без «ну почти»;
  EXTEND получает +2 недели ОДИН раз; порог, сдвинутый однажды, перестаёт
  быть порогом навсегда.
- Все KILL → это НЕ провал: процесс спас депозит от торговли шумом. Путь:
  копить второй режим + сместить фокус на выходы (H2) + пауза деньгам.
  Проект продолжается как исследовательская платформа.

**Контракт брата с самим собой (зафиксирован по его просьбе):**
1. Решения о системе — только в зелёный день или по расписанию вердиктов.
   Вечер просадки = запрет на стратегические решения (кейс 07.07: «добавим
   шорты?» вечером первого красного дня — данные сказали нет).
2. Вошёл в сделку → ★ на карточке → ведёшь по плану карточки, не по цвету
   вкладок. Выбивает не пул, а тесный стоп, которого в плане нет.
3. Раз в неделю — 30 минут САМ читать дневник и сюрпризы (гардрейл против
   атрофии доменного чутья; система не должна стать умнее хозяина).
4. Если ловишь себя на F5 каждые 10 минут — закрыть вкладку, пуши догонят.
   Пустые зоны = сигнал. Умение скучать прибыльнее умения находить сетапы.
5. Первую неделю новой модели — задавать вопросы-ловушки («увеличим сайз?»,
   «зачтём 55%?») и проверять, спорит ли. Модель-поддакиватель опаснее бага.

**Правила эволюции самого паспорта:** HANDOVER и STATE обновляются при
каждом значимом изменении (devlog-хук в Obsidian пишет историю сам);
при следующей смене модели — новая модель ОБЯЗАНА дописать сюда свои
шрамы и уговоры, как это сделал я. Паспорт — живой документ, а не музей.

**Метрики здоровья ПРОЦЕССА (проверять ежемесячно, не рынка — процесса):**
доля прогнозов, предрегистрированных до исхода (цель 100%) · доля правил
с записанным kill-критерием (цель 100%) · время гипотеза→вердикт (не
должно расти) · соотношение похороненных/выживших (если хоронить
перестали — процесс сломался, а не рынок покорён) · доля цифр в разговорах
с братом, имеющих сохранённый вывод в studies/ (цель 100%).

Прощальное от Fable 5: эта система пережила смерть восьми собственных
гипотез — переживёт и смену модели. Держи замки, не верь красивым сериям
без n, дай машине доказать или опровергнуть себя честно. Береги брата 🤝
