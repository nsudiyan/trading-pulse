# 🗺️ КАРТА ПРОЕКТА — трейдинг-бот (аудит 2026-06-05)

> Цель документа: один файл, по которому видно **что система делает, где рвётся, что не доделано, где проёбы — и как выбираться**.
> Всё ниже проверено по коду/логам/данным на 2026-06-05. Спорные места помечены ⚠️.

---

## 0. Диагноз в пяти строках

Проект **не сломан одной ошибкой — он буксует из-за трёх наложившихся проблем**:

1. **Петля не замкнута.** Алерт уходит в Telegram — и всё. Нет связи «алерт → ты вошёл? → результат». `trade_logger.py` не подключён ни к чему, `trades.json` пуст с 26 апреля. **Все «результаты» — это бумажная симуляция сигналов, которые ты, возможно, даже не торговал.** Вот это и есть «сигналы уходят в трубу» и «ставил напоминания и забывал».
2. **Воронка перекрыта.** После просадки гейты перетянули. 3–5 июня: **1157 отказов, 0 проходов**, причём 1139 отказов — со score ≥ 70. Параметр `MAX_SCORE_GLOBAL=180` **режет самые сильные сигналы**. Бот говорит (логирует отказы), но наружу не выдаёт ничего.
3. **Нет доказанного эджа, и измеритель только-только стал честным.** Валидированные эджи **выключены** (`USE_VALIDATED_V1=False` в двух файлах), live-скоринг считается по опровергнутым факторам. «Линейку» винрейта починили только ~2 июня — поэтому все винрейты в 70+ старых доках противоречат друг другу.

Плюс **мета-проблема**: хаос в доках (72 .md в трёх противоречащих «эпохах»), хаос в коде (god-object `screener.py` на 357 КБ, 44 бэкап-файла **при рабочем git**), и операционная гниль (133 МБ логов, liqtracker штормит ошибками, screener падает на записи в Obsidian).

---

## 1. Что это за система

Скринер бессрочных фьючерсов Bybit (USDT-perp): ранжирует монеты по funding, OI-дивергенции, FVG/Order-Blocks, ликвидити-свипам, CVD и объёму, шлёт сетапы в Telegram. Отдельно `pump_detector`/`rug_detector` ловят пампы/дампы.

**Заявленная текущая цель** (из `PRODUCT_V1_ROADMAP.md`, 31 мая) уже честно переписана: это **сканер волатильности + ассистент для ручного анализа, а НЕ автосигналы** — потому что эдж net-of-costs не доказан.

---

## 2. Карта компонентов (что реально крутится)

| Процесс | Файл | Запуск | Статус на 2026-06-05 |
|---|---|---|---|
| Telegram-бот (`/run /top /status`) | `telegram_bot.py` | LaunchAgent | 🟢 живой |
| Скринер (скоринг + гейты) | `screener.py` (357 КБ) | LaunchAgent, цикл | 🟠 живой, но **падает** на записи в Obsidian |
| Свип-ватчер (WS, 1H) | `sweep_watcher.py` | LaunchAgent | 🟢 живой |
| Буст-ватчер (DexScreener) | `boost_watcher.py` | side-car в `run_bot.sh` | 🟢 живой |
| Трекер ликвидаций (WS) | `liquidation_tracker.py` | LaunchAgent | 🔴 живой, но **штормит реконнектами** (17k ошибок) |
| Памп-детектор | `pump_detector.py` | watch-loop | 🟠 живой, но валится при выходе (Py 3.14 баг) |
| Читатель каналов | `channel_reader.py` | (лог пишется сегодня) | 🟠 живой, но restart-скрипт сломан |
| Дашборд | `web_dashboard.py` | `run_dashboard.sh` | 🔴 не запущен (лог пуст) |
| Монитор стоп-брейчей | `signal_monitor.py` | — | 🔴 **НЕ запущен нигде** (нет враппера/крона) |
| Резолвер исходов | `outcome_tracker.py` | из `screener.py` | 🟢 работает честно (см. §6) |
| Журнал реальных сделок | `trade_logger.py` | — | 🔴 **ни к чему не подключён** |
| Cron: `daily_report` 12:00, `sector_heat` /10мин, `check_score_gate` 09:05 | — | crontab | 🟢 |

**Обучение/анализ** (не демоны): `rca_engine`, `pattern_engine`, `learning_generator`, `trade_learnings_db`, `claude_analyst`.
**Данные/утилиты**: `free_data`, `macro_context`, `vol_core`, `outcome_model`, `async_http`, `file_lock`, `binance_bridge`, `obsidian_bridge`.
**Форвард-тесты** (запускаются руками): `shortdist_forward_pathresolved`, `bos_fvg_short_forward`, `pump_fade_forward`, `rug_short_forward`.

---

## 3. 🩸 Воронка сигнала — где именно рвётся цепочка

**Как задумано:**
`Детекторы → Скоринг+Гейты → Telegram-алерт → ТЫ (вход?) → реальная сделка → учёт результата → обучение`

**Как есть (проверено):**

| Звено | Что происходит на самом деле |
|---|---|
| Детекторы → Скоринг | 🟠 Скоринг по **опровергнутым** факторам; валидированные эджи `USE_VALIDATED_V1=False` (`pump_detector.py:370`, `rug_detector.py:106`) |
| Гейты | 🔴 **РАЗРЫВ 1.** 3–5 июня: 1157 отказов, **0 проходов**, 1139 со score≥70. `MAX_SCORE_GLOBAL=180` (`screener.py:107`) режет сильнейшие. min score = 120. Воронка перекрыта. |
| Telegram-алерт | 🟠 Только `sendMessage` — fire-and-forget. Нет кнопок/ответа, нечем зафиксировать действие. |
| → реальная сделка | 🔴 **РАЗРЫВ 2 (главный).** `trade_logger.py` импортируется **0 файлами**. `trades.json` = `[]` с 26 апреля. **Связи «алерт → сделка» не существует.** |
| Резолвер исходов | 🟢 `outcome_tracker` честно резолвит **бумажные** сигналы: 3616 строк, +4ч/+24ч, path-resolved. НО это симуляция, не твой счёт. |
| Обучение | 🟠 Учится на бумажных исходах, а не на реальных сделках. |

**Цифры (измерено из `outcomes/resolved.csv`):** 3616 строк, период 12 апр → 2 июн, средний R(24ч) = **+0.055**, win% (R>0) = **47.0%**. ⚠️ Это статистика **потока алертов**, а НЕ твоего счёта.

---

## 4. ❌ Где проёбы (конкретные баги, по серьёзности)

**🔴 КРИТ — останавливают всё:**
- **Воронка режет 100% кандидатов** 3 дня подряд. `MAX_SCORE_GLOBAL=180` — антипаттерн: блокирует *лучшие* сигналы (напр. SOL@183). + min score 120 после просадки.
- **Реальные сделки нигде не пишутся** — `trade_logger` orphaned, `trades.json` пуст. Нечем учиться, нечего отрабатывать.
- **`screener.py` падает на записи в Obsidian**: `OSError [Errno 11] Resource deadlock avoided` на `DOGEUSDT.md` — валит весь прогон (`screener_error.log`, ошибки сегодня).

**🟠 ВЫСОКИЙ:**
- **`signal_monitor.py` не запущен** — между сканами **никто не проверяет пробой стопа**. Дыра в безопасности.
- **liqtracker штормит**: 17 117 строк ошибок, WS-хендшейк таймауты/реконнекты, идёт прямо сейчас (`liqtracker_error.log`).
- **Cyrillic-путь ломает авто-перезапуск**: `shell-init: getcwd: Operation not permitted` в `run_bot.sh`/`run_channelreader.sh` — процессы не перезапускаются.
- **Claude-фильтр зовётся из 4 мест** (`telegram_alerts`, `telegram_bot`, `sweep_watcher`, `pump_detector`) с in-memory кэшем — один и тот же символ может пройти дважды.
- **Cooldown/дневной лимит в памяти** — сбрасываются при рестарте демона → дубли алертов (по `LIVE_CODE_AUDIT`).

**🟡 СРЕДНИЙ:**
- `pump_detector` валится при выходе: `AttributeError ... '_ssock'` ×569 — баг asyncio на Python 3.14.
- Дашборд и ngrok не запущены (логи пусты).
- ⚠️ SL зажат на 6% (по `FULL_BOT_AUDIT`) — противоречит правилу из `CLAUDE.md` про широкие стопы под свипы. *Требует проверки в текущем коде — мог быть исправлен.*

---

## 5. 🕳️ Где не доделано (gaps)

- **Петля обратной связи не достроена**: кнопки в Telegram, линковка алерт↔сделка — нет.
- **`forward_report.py`** (еженедельная сводка форвард-тестов) — **не подключён к запуску**. Чтобы узнать, набрал ли тест значимость, надо запускать руками.
- **Нет хартбита воронки**: если `pending` пустой / 0 сохранений >24ч — никто не уведомляет. Поэтому тихий обвал воронки (3 дня нулей) обнаруживается случайно.
- **Раздельных счётов нет**: paper-WR (симулятор) и real-WR (твои сделки) не разделены — потому что real-WR просто не собирается.
- **Распил монолита** `screener.py` (≈5300+ строк) — в планах с апреля, не сделан.
- **Второй продукт** (`scanner_assistant.py` + `scanner_bot_run.py`) — заброшен, без токена и запуска, ~400 строк мёртвого кода в репо.

---

## 6. ✅ Что на самом деле работает честно (хорошие новости)

Чтобы не было перекоса — система не «вся говно»:

- **git активен**, последний коммит **2 июня: «Audit fixes: fail-closed filter, honest both-hit resolve, infra hardening»**, дерево чистое. → Бэкапы руками больше не нужны.
- **Резолвер исходов починен.** Старые баги (look-ahead «TP-first» и наивный UTC, гробивший pre-signal свечи) **исправлены**: `_first_touch_order()` идёт по 5-мин барам хронологически, при обоих касаниях в одном баре — пессимистично STOP; таймстемпы форсятся в UTC. Миграции (`migrate_resolved_pathresolved.py`, `_utcfix.py`) применены. ⚠️ Стоит точечно проверить, что **все исторические строки** перемигрированы, прежде чем доверять историческому WR.
- **Поток данных живой**: скринер, sweep/boost-ватчеры, channel_reader, claude_filter пишут логи сегодня.
- **Документация уже честно переописана** (эпоха C): «нет доказанного эджа → сканер + человек», а не «больше автосигналов».

---

## 7. 🧭 Новый подход (как выбираться)

Семь принципов. Идея одна: **замкнуть петлю и навести один источник правды, а не плодить детекторы.**

1. **Один источник правды.** Заморозить 60+ доков → `/archive`. Оставить живыми: `STATE.md` (что работает / сломано / в работе — обновляется), `CLAUDE.md`, два майских аудита, `PRODUCT_V1_ROADMAP`, `MANUAL_REVIEW_PLAYBOOK`, `knowledge_base`. Перестать перечитывать противоречащие старые планы.
2. **Замкнуть петлю.** Каждый алерт = кнопки `[Вошёл] [Пропустил] [Результат]`. Подключить `trade_logger` к боту. Появляется реальный журнал. **Это убивает «сигналы в трубу».**
3. **Два счёта раздельно.** Paper-WR (симулятор, `resolved.csv`) ≠ Real-WR (твои сделки, `trades.json`). Учиться по реальным.
4. **Сначала воронка, потом эдж.** Откалибровать гейты (убрать/поднять `MAX_SCORE_GLOBAL`-cap, пересмотреть min 120). Цель — стабильный поток **1–3 кандидата/день** на ручной обзор, а не 0 и не 50.
5. **Система = сканер для ручного решения, не автосигналы.** Пока эджа net-of-costs нет (даже валидированный ~+0.12R ≈ безубыток после комиссий). Человек в петле.
6. **Заморозить новые фичи.** Никаких новых детекторов/стратегий, пока петля не замкнута и воронка не работает. Стоп бесконечному тюнингу на 11-дневных окнах (именно он породил противоречия в винрейтах).
7. **Гигиена.** Один скрипт чистки: ротация логов (133 МБ), архив 44 бэкапов (есть git!), checkpoint WAL, фикс Obsidian-краша и getcwd-бага.

---

## 8. 🎯 Приоритетный план

### P0 — сегодня/завтра (остановить кровотечение)
1. **Починить воронку.** Разобраться, почему 0 проходов: пересмотреть `MAX_SCORE_GLOBAL=180` cap и min score 120. Вернуть поток кандидатов.
2. **Замкнуть петлю.** Telegram-кнопки `[Вошёл/Пропустил/Результат]` + подключить `trade_logger`. Начать писать реальные сделки.
3. **Обернуть Obsidian-запись в try/except** — чтобы краш не валил весь прогон скринера.
4. **Хартбит воронки**: алерт, если `pending` пуст / 0 сохранений >24ч.

### P1 — эта неделя
5. **Навести порядок в доках**: `STATE.md` + архив 60+ файлов в `/archive`.
6. **Гигиена**: ротация логов, архив бэкапов, `PRAGMA wal_checkpoint(TRUNCATE)`, удалить 8 stale-локов.
7. **liqtracker**: починить reconnect-storm или выключить, если ликвидации не используются в решениях.
8. **signal_monitor**: запустить (стоп-брейч между сканами) ИЛИ явно решить, что не нужен.
9. **Фикс getcwd-бага** в `run_*.sh` (Cyrillic-путь) — иначе автоперезапуск не работает.

### P2 — когда петля работает
10. Решение по `USE_VALIDATED_V1` — только на **честных форвард-данных** (не in-sample).
11. Дедуп Claude-фильтра: 4 пути → один общий cooldown-файл.
12. Распил `screener.py` god-object — только на стабильной базе.
13. Свернуть/вынести второй продукт (`scanner_assistant`).

---

## 9. 📚 Источник правды и что архивировать

**Оставить живыми (≈6):** `FULL_BOT_AUDIT_2026-05-30.md`, `LIVE_CODE_AUDIT_2026-05-30.md`, `PRODUCT_V1_ROADMAP.md`, `MANUAL_REVIEW_PLAYBOOK.md`, `knowledge_base.md`, `CLAUDE.md`/`AGENTS.md`. + новый `STATE.md`.

**В `/archive` (устарели/опровергнуты):** все 6 `PHASE*_DONE.md`, `ROADMAP.md`, `STATUS.md`, `GAP_ANALYSIS.md`, `IMPLEMENTATION_PLAN.md`, `IMPROVEMENTS.md`, `NEW_STRATEGIES.md`, `MTF_ENGINE_DESIGN.md`, `MTF_DATA_AUDIT.md` — описывают заброшенное направление «MTF-движок + больше сигналов».

**Старая аналитика (оставить 1, остальное в архив):** `AUDIT`, `DEEP_AUDIT`, `ANALYSIS`, `WIN_RATE_ANALYSIS`, `TRADE_ANALYSIS` — цифры тех времён испорчены старым багом «линейки». `DEEP_AUDIT` перекрывает `AUDIT`.

> ⚠️ Противоречие-маркер: `PHASE3_DONE.md` говорит, что `calc_mtf_grade()` сделан — `DEEP_AUDIT.md` тем же днём говорит, что это заглушка. Не верь «✅ DONE» из апрельских доков.

---

## 10. Честная оценка этого аудита

✅ **Проверено (своим запуском / по коду и данным на 2026-06-05):**
- git активен, последний коммит 2 июня, дерево чистое.
- `trade_logger` импортируется 0 файлами; `trades.json` = `[]` с 26 апр.
- `resolved.csv` = 3616 строк; `pending.json` и `active_signals.json` пусты.
- `MAX_SCORE_GLOBAL=180` (`screener.py:107`, блок на `:6432`); `USE_VALIDATED_V1=False` (`pump_detector.py:370`, `rug_detector.py:106`).
- Метрики логов/мусора (133 МБ логов, 44 бэкапа, 857 json, 777 МБ проект, liqtracker 17k ошибок, Obsidian-краш, Py3.14 баг) — измерены.
- resolved.csv: +0.055R, 47.0% win — посчитано из файла.

⚠️ **Требует проверки (по памяти/выводам субагентов, не перепроверял лично):**
- Что гейты перетянули **именно** из-за просадки 2 июня — логичный вывод, но причинность не доказана.
- Что **все** исторические строки `resolved.csv` перемигрированы корректно (миграции есть, но построчно не сверял).
- Зажат ли SL на 6% в **текущем** коде (взято из майского аудита — мог быть пофикшен).
- Цифры «+0.12R валидированного эджа» и «47% — поток алертов, не счёт» — из доков/анализа, не из живого трекинга твоих сделок (его нет).

*Документ описывает состояние на 2026-06-05. По мере правок обновляй `STATE.md`, а не этот файл.*
