# 🔬 Доскональный аудит всего бота — 2026-05-30

**Метод:** 42 агента, 4 фазы (карта→аудит 5 измерений→состязательная верификация→синтез), 548 чтений кода, read-only. 29 находок подтверждено, 1 опровергнута. Полный лог: task w6d8fpig7.

## Итог (headline)
Brutally honest bottom line: this is a competent-looking alert bot whose ability to catch pumps/dumps is structurally capped LOW and is being measured with a broken ruler. The one OOS-validated pump edge (vol_core: vol_ratio vs 30-day median) is DEAD in production (USE_VALIDATED_V1=False in both pump_detector.py:275 and rug_detector.py:106 — grep finds zero `=True`); the one validated short edge (btc_cascade lift 5.12) is DEAD inside the same disabled rug block. So every live alert is scored on a hand-tuned additive sum built substantially from DEBUNKED inputs (Fear&Greed +10, whale pos-skew +20, SMC FVG/OB/CHoCH ×1.2, CVD-divergence patterns), and that sum is anti-predictive at the top by the bot's own measurement. Worse, the WIN/LOSS ruler is corrupted: the host is MSK (+0300) and the screener resolver (outcome_tracker.py) parses naive-UTC run_ts strings then calls .timestamp(), shifting the 4h kline window back 3h to [run-3h, run+1h] — it grades signals mostly on PRE-signal candles. Empirically the pump catcher has resolved only 57 of 151 alerts at 4h with 6 WIN/51 LOSS (~10.5% WR vs the +12%/4h bar), consistent with ALIGN1 (0.5% trigger chasing a 12% target). The catching philosophy is contradictory (detect-pre-move via 'price hasn't moved' gates, then chase a token 0.5%/0.2% confirmation), the architecture has real FD-leak and singleton-bypass seams, and the live sweep module emits nothing. Net: the edge is thin and mostly unrealized; the priority is fix the ruler, ship the validated factors, and strip the debunked noise — NOT add features.

## Классификация сигналов

**✅ VALIDATED (но в live ВЫКЛЮЧЕНЫ):** vol_core conviction (vol_ratio_1h/4h + vol_spike_count vs 30-DAY rolling median) — pump_detector.py:1247-1262 / vol_core.py:69-109 — but DEAD (USE_VALIDATED_V1=False @pump_detector.py:275); contributes ZERO to every live pump alert; vol_accel = vr1/vr4 > 1.2 (fresh-spike, modest semi-orthogonal) — lives only inside the dead vol_core path; stablecoin_inflow (event-study lift 2.31) — pump_detector.py:1269-1275 fully COMMENTED OUT ('not yet wired'); only reaches the Claude prompt via macro_context.py:86, never scores; btc_cascade for SHORTS (BTC 1h<=-1.5% → +30, lift 5.12) — rug_detector.py:548-552 — DEAD inside USE_VALIDATED_V1=False block (rug_detector.py:106); the ONLY validated factor rug_detector has

**❌ DEBUNKED / UNVALIDATED (в live АКТИВНЫ):** DEBUNKED Fear&Greed: pump_detector.py:1286-1289 still adds +10 (FNG_PUMP_FEAR_BONUS / FNG_RUG_GREED_BONUS) AFTER the MIN_SIGNAL_SCORE gate, before the min(score,130) clamp — can promote a borderline candidate; event-study lift 1.0; DEBUNKED whale-tracking (pump): pos_skew +20/-15/+10 at pump_detector.py:1428-1440 — applied AFTER clamp (1291) and read by the conviction gate (1995); a debunked +20 can push final_score over CONVICTION_MIN_SCORE=65; DEBUNKED whale/absorption (screener breakout): +22 whale Buy / +18 absorption at screener.py:3288-3297; DEBUNKED SMC FVG/Order Block: screener.py:2935-2951 (squeeze +18/+18), 3091-3103 (bos_fvg +22/+22 direction-agnostic), 3554-3584 (short_dist +18/+18) — largest debunked point-weight exposure in the system; DEBUNKED SMC CHoCH used MULTIPLICATIVELY x1.2: screener.py:3152-3155 (bos_fvg), 3402-3405 (breakout) — scales the WHOLE setup 20% on a debunked condition; also relaxes the TG min-score by 30 at 6413-6414; DEBUNKED CVD-divergence baked into candidate generation: pump_detector CVD_BULL/BEAR_DIV base +40 (1117/1138) and SLOW_DIST CVD bonus +20 (1216-1221); screener squeeze/breakout CVD terms; UNVALIDATED funding-regime table is the DOMINANT lever (squeeze pressure_building +60, short_dist high_longs_building +52) — screener.py:2357-2371; never OOS-confirmed standalone, magnitude decides which setup wins; UNVALIDATED pump base pattern scores (the whole candidate set): SHORT_SQUEEZE +45, POST_PUMP_RUG +50, VOLUME_SURGE +38, CVD +40, ATR_COIL +36, SLOW_DIST +40 — hand-tuned, untested; DEBUNKED 'high score = better' still drives both gates monotonically (CONVICTION floor + screener best=max) despite the in-prompt admission that 120+ → WR~50%; rug_detector live score: supply%/age/listings/DEX/TVL heuristics (rug_detector.py:490-521) — none on the validated list; combined age+supply bonus (518-521) TRIPLE-counts the same two facts

**🔁 NOISE / DOUBLE-COUNT:** OI triple-counted in screener: _OI_SCORE regime table + oi_div + magnitude/coil scored on top of each other per setup (squeeze 2890+2895-2902; breakout 3264-3267+3308-3317) — same correlated OI read summed as independent; OI gate-PLUS-bonus in pump_detector: same OI variable both filters entry and adds points repeatedly (gates at 963/1043/1093 + bonuses at 1061/1121/1142/1182/1224 + Binance cross-exch +15 at 1007-1010, ~collinear); Liquidations triple-counted in pump_detector: Bybit-5m +25 (992-994) THEN Coinalyze-60m +20/+10 (997-1004) over NESTED windows = same cascade scored up to +45; same on rug side (1066-1078); screener volume double-count: vol_ratio (3300-3305) stacks with vol_accel (3280-3285) in breakout — two volume terms from correlated data; screener 'Layer-3 one-path' only de-dups OI magnitude; oi_div and volume still enter >=2x per setup (the 'orthogonal' claim at comment 2374 is false in trending regimes); Three disagreeing WIN/LOSS rulers writing two CSVs: outcome_tracker 5-symbol {TP1,STOP,WIN,LOSS,FLAT} optimistic tie-break vs outcome_model 3-symbol order-aware pessimistic vs migrate's hardcoded copy; 1h ruler magic numbers triple-duplicated: pump_detector.py:707-713, shadow_analyze.py:122-127, migrate_pump_resolved_win_fix.py:30-33 — outcome_model has NO 1h constants

## Вердикт: скоринг
FACT: the score is a flat additive sum and is anti-predictive at the top by the bot's own in-prompt measurement (claude_realtime_filter.py:255-261: score 50-70→WR~74%, 120+→WR~50% coin-flip), yet both gates still threshold/rank on it monotonically (pump_detector `score += N` everywhere; CONVICTION gate on final_score @1995; screener best=max(scores) @3672). The additive model double/triple-counts correlated reads — OI (gate + multiple bonuses + cross-exchange), liquidations (Bybit-5m + Coinalyze-60m over NESTED windows = same cascade up to +45), volume (vol_ratio + vol_accel), funding (regime table is the single dominant lever at +60/+52 and is UNVALIDATED). So a single big event manufactures large score with little added information, which is precisely the mechanism that makes high scores anti-predictive. UPDATE: CONVICTION_MIN_SCORE was reverted 85→65 today (pump_detector.py:118) — better than 85 (which sat in the coin-flip region) but still a monotonic FLOOR, so it still admits the 120+ region and is not the non-monotone 50-90 sweet-spot band the data implies. The re-gating gap (post-clamp, post-gate edits from DEBUNKED whale-tracking and unvalidated L2) further corrupts the scalar the gate reads. Verdict: the scoring is unsound as a ranker. Short-term: convert score to a non-monotone eligibility band (block <50 AND >120), remove debunked additive terms (FNG, pos-skew, SMC FVG/OB, CHoCH multiplier), and de-dup OI/liq/volume into one term each. Long-term: replace the sum with a calibrated logistic on de-correlated validated features and select by probability — but only after the ruler (BUG1) is fixed, because every calibration target today is corrupted.

## Вердикт: ловля пампов/дампов
FACT + HYPOTHESIS. The catching philosophy is internally contradictory: patterns fire on 'price hasn't moved' gates (squeeze price_chg_30m<=3, VOLUME_SURGE abs<=1.0, CVD_BULL<=0.3) to catch early, but send is then blocked in _check_momentum until move_pct>=MOMENTUM_PCT=0.50% AND a 5m burst>=0.20% (pump_detector.py:1781/1817) — so the candidate is identified pre-move but only alerted after a token post-move confirmation. That confirmation (0.50%) is ~24x smaller than the WIN bar (+12%/4h, +15%/24h), and nothing estimates expected magnitude, so precision against the +12% ruler is structurally capped low. NEW EMPIRICAL CONFIRMATION: pump_resolved.csv has 151 alerts, only 57 resolved at 4h → 6 WIN / 51 LOSS / 93 FLAT, i.e. ~10.5% WR on decided 4h pump outcomes against the +12% bar (FACT from the artifact; small n, single regime). This is consistent with the established 'standalone vol edge ≈ flat, real +15%/24h pump-rate ~5%' and with ALIGN1 — most 0.5% confirmations never reach +12%. The catcher is therefore neither genuinely early (it chases a confirmation) nor safely confirmed (the confirmation is meaningless in magnitude), and it pays latency for both. It is NOT over-reliant on a single noisy feature — it is reliant on a hand-tuned blend whose strongest empirically-validated component (vol_core, btc_cascade) is switched OFF. Verdict: catching is weak and mostly unrealized; the fix is to (1) ship the validated vol/cascade factors as a pre-GATE so a pattern cannot fire without a real 30-day-median spike, and (2) make the confirmation magnitude-aware (require a plausible path toward the WIN threshold), NOT to add patterns.

## Вердикт: архитектура
FACT. The architecture has four real seams, all verified: (1) FETCH SEAM — pump_detector runs 10+ bare requests.get calls (incl. the per-symbol hot paths _fetch_oi_5m@792, _fetch_trades_raw@811, kline-extremes@617) with default keep-alive, bypassing the Connection:close FD-leak fix that screener.SESSION and vol_core._SESSION apply; screener's own comment blames pump_detector for the EMFILE leak. (2) CLAUDE-CLIENT SEAM — the get_client() singleton (built specifically to stop CLOSE_WAIT/'too many open files') is bypassed by channel_reader.py:509/1836 (new _ant.Anthropic per call) and signal_monitor.py:98/669 (raw HTTP). (3) MEASUREMENT SEAM — two CSVs with different schemas and WIN vocabularies, three disagreeing rulers, and the single-source-of-truth outcome_model used by only one pipeline; migrate reimplements it; the 1h ruler is triple-duplicated. (4) FLAG SEAM — USE_VALIDATED_V1 is two independent False flags, so shipping the validated edge requires editing two files, and it never has been. There is also a dead-code mass in pump_detector (153-271: PRICE_POS/ABSORB/WHALE/SYN_*/ANTI_*/BREAKOUT_* constants and _cvd_pts/_liq_cluster_score/fetch_crypto_panic_sentiment helpers — defined, never called) whose docstrings describe penalties that do not exist in the live path, which is a documentation-vs-reality trap for any future maintainer. Verdict: the architecture is functional but leaky and under-unified; the correctness seams (measurement ruler, flag seam) are higher priority than the resource seams (FD/client) because they silently corrupt decisions rather than crash.

## Топ-баги

- **[critical] ACTIVE (not latent): screener WIN/LOSS ruler reads PRE-signal candles — host is MSK(+0300), naive-UTC run_ts shifts the 4h window to [run-3h, run+1h]**
  - ev: FACT. outcome_tracker.py:178-183 _now_ts/_parse_ts build NAIVE datetimes (datetime.utcnow().strftime + strptime, no tzinfo). check_and_resolve (:426) passes run_dt=_parse_ts(entry['run_ts']) to _fetch_klines_extremes (:432), which does int(from_dt.timestamp()*1000) (:151). Confirmed empirically: `date` = MSK +0300; python time.timezone/3600 = -3.0; for run_ts '2026-04-12T09:56:48' naive.timestamp()=1775977008 vs UTC-aware 1775987808 → delta EXACTLY -10800s (-3h). resolved.csv run_ts has no Z/offset (sample '2026-04-12T09:56:48'). So the [run, run+4h] window becomes [run-3h, run+1h] in true UTC — it grades on 3h of pre-signal candles and only 1h after. Pump CSV is SAFE: pump_detector._fetch_kline_extremes (:610) takes epoch int p['ts'] and does from_ts*1000 directly (:625) — no naive conversion.
  - fix: In outcome_tracker.py parse run_ts as UTC-aware: datetime.strptime(...).replace(tzinfo=timezone.utc) (or store run_ts with '+00:00'), and use that for the window. After fixing, RECOMPUTE resolved.csv from scratch — all 3109 existing TP1/STOP/WIN/LOSS labels are corrupted; every downstream stat (screener WR, RCA, channel accuracy, analyze_patterns, audit kill-switch) was built on them.
- **[high] daily_report screener WR is doubly wrong: drops 24% of rows (all TP1+STOP) AND skews the time-window by the same naive-UTC bug**
  - ev: FACT. daily_report.py:104-110 increments wins only on outcome_4h=='WIN', losses only on 'LOSS' — TP1 and STOP are never bucketed. Quantified against outcomes/resolved.csv: 378 TP1 + 357 STOP = 735 of 3109 rows (24%) dropped from the denominator; reported WR = 634/(634+694)=47.7% over the ±0.5%-drift subset only, while TP1+WIN=1012 vs STOP+LOSS=1051 over the true decided population. Separately, daily_report.py:90 cutoff uses true-UTC datetime.now(timezone.utc).timestamp() but :99 parses run_ts via fromisoformat (no Z present) → naive → mis-includes/excludes rows by 3h on this MSK host.
  - fix: In daily_report._outcome_stats map TP1→win and STOP→loss for the screener CSV (branch by csv_path; pump CSV has no TP1/STOP). Parse run_ts as UTC-aware before .timestamp().
- **[critical] VALIDATED edges are entirely OFF in production — bot scores live alerts only on debunked/unvalidated factors**
  - ev: FACT. grep `USE_VALIDATED_V1 = True` returns zero. pump_detector.py:275 and rug_detector.py:106 are SEPARATE False flags (two files). The dead pump V1 block (1247-1262) is the only caller of vol_core.conviction; the dead rug V1 block (527-606) holds btc_cascade +30 (548-552). stablecoin_inflow +20 is commented out (pump_detector.py:1269-1275). So the only OOS-confirmed pump edge AND the only validated short edge contribute ZERO; live scoring rests on FNG/whale/SMC/CVD-div (all debunked) + funding-regime/base-patterns (unvalidated).
  - fix: Hoist USE_VALIDATED_V1 into one shared config controlling both files. After a shadow A/B, enable it so the validated vol factor and btc_cascade enter scoring; gate weak/below-tier as a SECOND cut, not additive. Wire stablecoin_flow.get_stable_flow() into scan_once pre-fetch for the +20.
- **[high] Re-gating gap with the now-lowered floor: DEBUNKED whale +20 is applied AFTER clamp and read by the conviction gate — and CONVICTION_MIN_SCORE was reverted to 65 today**
  - ev: FACT + UPDATE. Order is: min(score,130) clamp at pump_detector.py:1291, THEN pos_skew r['score']+=20 (DEBUNKED whale) at 1429 and orderbook ±10/+5 at ~1473-1512 — no re-clamp/re-gate. final_score=c['score']+dex_adj (1989) is read by the conviction gate at 1995. CONVICTION_MIN_SCORE=65 NOW (pump_detector.py:118, comment dated 2026-05-30 reverting 85→65 after credit top-up) — lower floor means a debunked +20 more easily promotes a borderline candidate. Also _promote_wait_watchlist (1865) calls send_pump_alert directly (1911), BYPASSING the conviction/time/DEX gates entirely for WAIT-list promotions.
  - fix: Apply ALL contributions BEFORE selection, then gate+clamp ONCE on the final value. Remove pos_skew from scoring (keep as Claude context only). Route promoted WAIT candidates through the same conviction/time gates as the watch() path.
- **[high] Live sweep module emits nothing: sweep_watcher labels candidates 'range_sweep' which the RT filter hard-SKIPs before any Claude call**
  - ev: FACT. sweep_watcher.py:161 sets _cand['setup']='range_sweep' then calls filter_candidate (159) under default CLAUDE_RT_FILTER=on (157). claude_realtime_filter.py:809 WEAK_SETUPS={breakout,swing,range_sweep} → instant pre_filter SKIP at 810, no Claude call. screener's range_sweep is force-zeroed (screener.py:3241) and TG-min 9999. So the ONLY active sweep path is dead-on-arrival in default config — the module runs, detects, scores, then is blocked at the gate.
  - fix: Exempt the sweep source from the WEAK_SETUPS pre-filter (it is a different signal than screener breakout/swing), OR stop labeling it 'range_sweep' before the RT call. Otherwise document it only works with CLAUDE_RT_FILTER=off.
- **[high] FD-leak fix bypassed on pump_detector's hottest paths (bare requests.get keep-alive)**
  - ev: FACT. pump_detector uses `import requests as _req` + bare _req.get at 10+ sites incl. _fetch_kline_extremes(617), _fetch_oi_5m(792), _fetch_trades_raw(811) — these run per-symbol per-scan/resolve with default keep-alive and NO Connection:close, unlike screener.SESSION (178-186) and vol_core._SESSION (113-133). screener.py:180 comment explicitly blames pump_detector's 300s scan for the EMFILE leak, yet its own OI-5m/trades/kline calls still run unfixed.
  - fix: Route pump_detector's bare _req.get through screener.SESSION (already imported and used for some calls) or give pump_detector its own Connection:close session. Prioritize _fetch_oi_5m and _fetch_trades_raw (per-symbol per-scan).
- **[high] SL clamp [1,6]% makes the validated wide ~8% structural stop unreachable**
  - ev: FACT. claude_realtime_filter.py:742 sl_pct=max(1.0,min(6.0,...)); telegram_alerts.py:111/114 applies the clamped value to price with no second clamp; the prompt itself only asks for 1.5-5.0 (claude_realtime_filter.py:333), reinforcing tight stops. Claude cannot emit an 8% structural SL — silently capped at 6% — so liquidity-sweep-surviving stops (a stated user requirement) are impossible.
  - fix: Raise the sl_pct cap to ~10% (or make it setup/ATR-aware) and align the prompt range. Keep TP/SL R:R sane by widening tp_pct accordingly.
- **[high] outcome_model single-source-of-truth bypassed by migrate + 1h ruler triple-duplicated**
  - ev: FACT. migrate_pump_resolved_win_fix.py:36 reimplements outcome() verbatim and hardcodes thresholds (25-33), importing NOTHING from outcome_model. The 1h ruler (3%/2% long, 3%/2% short) is hardcoded in pump_detector.py:707-713, shadow_analyze.py:122-127, and migrate :30-33; outcome_model.py defines ONLY 4h constants (15-18). Currently magnitude-consistent but structurally decoupled — any change to outcome_model silently won't propagate, re-introducing the train/live drift the module was built to prevent.
  - fix: Add PUMP_TP_1H_PCT/PUMP_SL_1H_PCT/RUG_TP_1H_PCT/RUG_SL_1H_PCT to outcome_model.py; import outcome() and all constants in migrate + shadow_analyze + pump_detector.

## Приоритизированный план действий

1. **[risk=medium]** Fix the naive-UTC parse in outcome_tracker.py (parse run_ts as timezone-aware UTC before .timestamp()), then RECOMPUTE resolved.csv from scratch. Apply the same UTC-aware parse in daily_report.py:99.
   - FACT: host is MSK(+0300), so the screener 4h window is currently [run-3h, run+1h] — labels are graded on pre-signal candles. This corrupts ALL 3109 resolved rows and every stat built on them (WR, RCA, channel accuracy, analyze_patterns, audit kill-switch). Nothing downstream can be trusted until this is fixed. Highest impact, highest confidence.
2. **[risk=low]** In daily_report._outcome_stats, map TP1→win and STOP→loss for the screener CSV (branch by csv_path).
   - FACT: 735/3109 rows (24%, all TP1+STOP) are dropped from the WR denominator today, so the daily WR shown is computed over only the ±0.5%-drift subset — exactly the kind of inflated metric the user has been burned by. Cheap, high-confidence.
3. **[risk=medium]** Hoist USE_VALIDATED_V1 into one shared config; after a shadow A/B, enable it so vol_core conviction (pump) and btc_cascade (rug) enter scoring. Gate weak/below-tier as a 2nd cut, not additive. Wire stablecoin_flow into scan_once for the +20.
   - FACT: the ONLY OOS-validated pump edge and the ONLY validated short edge are switched off; live alerts run purely on debunked/unvalidated factors. This is the single biggest lever to move realized edge above break-even — consistent with 'stack validated selectivity'.
4. **[risk=medium]** Remove DEBUNKED additive terms: FNG +10 (pump_detector.py:1286-1289), pos-skew +20/-15/+10 (1428-1440), screener whale/absorption (3288-3297), and the CHoCH x1.2 multiplier (3152-3155, 3402-3405). Down-weight or remove SMC FVG/OB (2935-2951, 3091-3103, 3554-3584).
   - FACT: these are on the do-not-credit list yet are large levers (CHoCH multiplier scales whole setup 20%; SMC FVG/OB up to +22 stackable; whale +20 promotes over the now-65 floor via the re-gating gap). Removing them reduces noise and the confluence-trap that drives anti-predictive high scores. 'Reduce noise', not add features.
5. **[risk=medium]** Close the re-gating gap: apply all score contributions BEFORE selection, gate+clamp ONCE on the final value; route _promote_wait_watchlist sends through the same conviction/time gates as watch().
   - FACT: post-clamp/post-gate edits (debunked whale, unvalidated L2) mutate the scalar the conviction gate reads, and promoted WAIT candidates bypass conviction/time gates entirely (pump_detector.py:1911). The gate is only as honest as the value it reads.
6. **[risk=medium]** De-duplicate correlated scoring: fold OI magnitude+oi_div+coil into one signed OI term per setup; take a single max() USD figure across Bybit-5m and Coinalyze-60m liquidations; merge vol_ratio+vol_accel into one volume term.
   - FACT: OI counted ~2-3x, liquidations up to +45 from one nested-window cascade, volume twice. This double-counting IS the mechanism behind the anti-predictive top of the score distribution. Improves calibration without new data.
7. **[risk=medium]** Convert the conviction gate from a monotone floor to a non-monotone eligibility band (block <50 AND >120) instead of the current >=65 floor.
   - HYPOTHESIS (grounded in the bot's own in-prompt stat 50-70→WR74% vs 120+→WR50%): a floor still admits the proven coin-flip region. A band targets the empirically better zone. Re-validate the band thresholds AFTER the ruler fix, since current WR numbers are corrupted.
8. **[risk=low]** Raise the RT-filter sl_pct cap from 6% toward ~10% (setup/ATR-aware) and align the prompt range, so structural stops survive liquidity sweeps.
   - FACT: the [1,6]% clamp (claude_realtime_filter.py:742) silently caps the validated wide ~8% stop, and the user explicitly requires sweep-surviving stops. telegram_alerts applies it with no re-clamp.
9. **[risk=low]** Unify the measurement ruler: add 1h constants to outcome_model.py, import outcome()+constants in migrate/shadow_analyze/pump_detector, and decide one WIN vocabulary across both CSVs.
   - FACT: three disagreeing rulers + triple-duplicated 1h thresholds + migrate reimplementing outcome() = guaranteed future train/live drift, the exact failure outcome_model was created to prevent.
10. **[risk=low]** Fix the resource seams: route pump_detector's bare _req.get (esp. _fetch_oi_5m, _fetch_trades_raw, kline-extremes) through a Connection:close session; migrate channel_reader's two Anthropic instantiations to claude_client.get_client().
   - FACT: the FD-leak fix is bypassed on the highest-volume hot paths (per-symbol per-scan) and the Claude singleton is bypassed in 2-4 sites — both re-open the EMFILE/CLOSE_WAIT failure the fixes targeted. Lower priority than correctness because it crashes rather than silently corrupts, but still real.
11. **[risk=low]** Delete or clearly quarantine the dead-code mass in pump_detector.py:153-271 (unused constants + _cvd_pts/_liq_cluster_score/fetch_crypto_panic_sentiment) whose docstrings describe non-existent penalties.
   - FACT (grep-confirmed never referenced): it is a maintainer trap — the docstrings imply soft/hard rejects and absorption/whale/synergy logic that does NOT run, risking false mental models of what the bot actually scores. Pure hygiene, zero behavior change.

## Честные оговорки

- The most damning NEW finding (BUG1/BUG2 actively firing) depends on the host being non-UTC. I verified `date`=MSK +0300 and time.timezone=-3.0 and reproduced the exact -10800s shift on a real run_ts. If the production bot runs on a UTC host (e.g. a cloud VM), these two bugs are latent there even though they fire on this dev machine — but resolved.csv was clearly written on a +0300 host (file locale + timestamps), so the EXISTING 3109-row dataset is corrupted regardless of where the bot now runs.
- The pump catcher's ~10.5% 4h WR is FACT from outcomes/pump_resolved.csv, but n is tiny (57 decided of 151) and single-regime; treat it as directional, not a precise estimate. It also is NOT corrupted by BUG1 (the pump resolver uses epoch ints correctly), so it is the most trustworthy WR number in the system.
- I could NOT use CodeGraph (no .codegraph/ index in this workspace; status returned 'no project loaded'), so structural claims here come from direct Read/grep on the actual files, not the AST graph. Line numbers were re-verified against today's edited pump_detector.py (29 мая 01:43) and screener.py (29 мая 00:29) where cited.
- CONVICTION_MIN_SCORE is now 65 (reverted from the 85 the brief assumed), per a comment dated 2026-05-30 — so the 'gate pushes into the coin-flip region' framing is partially stale: 65 is better than 85 but still a monotone floor, so the core non-monotone-band recommendation stands.
- I did not independently re-validate the OOS edge magnitudes (vol_core 2x-relative, lift 2.31/5.12) — those are taken from the established session findings; my work confirms only that they are wired OFF in live code, which is the actionable part.
- 'Edge is thin' is the honest frame: even with every fix, the established realized edge on filtered signals is ~+0.12R (single regime, ~break-even after costs). Fixing the ruler and shipping validated factors is about not destroying a thin edge with corrupted measurement and debunked noise — it is not a path to a large, robust edge, and the recommendations deliberately avoid promising one.

## Все подтверждённые находки (29)

- **[critical] BUG1** (correctness_bugs): Screener outcome resolver fetches WRONG kline window on non-UTC hosts (naive UTC string → .timestamp() treated as local) — corrupts the WIN/LOSS ruler
  - ev: outcome_tracker.py:178-183 (_now_ts/_parse_ts produce naive datetime from datetime.utcnow().strftime), :151-152 (_fetch_klines_extremes uses int(from_dt.timestamp()*1000)), :421-433 (check_and_resolve passes run_dt=_parse_ts(...) to the fetch). Confirmed empirically: on TZ offset +3h, naive.timestam
  - fix: Store/parse run_ts as timezone-aware UTC (append '+00:00' or use datetime.now(timezone.utc)), or convert to epoch with calendar.timegm(dt.timetuple()) before building the Bybit start/end params. Other
- **[critical] VAL1** (signal_validity): VALIDATED volume edge (vol_core conviction) is DEAD — 0 live points
  - ev: pump_detector.py:1247 (USE_VALIDATED_V1 and signal_type=='pump'), flag=False @pump_detector.py:275; conviction deltas +5/+18/+33 @vol_core.py:96-109; v1_score_delta +15/+10/+8 @vol_core.py:69-78
  - fix: This is the ONLY OOS-confirmed pump edge (vol_ratio_1h/4h + vol_spike vs 30-day median, conviction tiers high~52%/standard~25%/weak~23% precision on test). It is entirely off in live scoring. The KEY 
- **[critical] SCORE1** (scoring_soundness): Score is a flat additive sum and is ANTI-predictive at the top by the bot's own measurement — yet both gates rank/threshold on it monotonically
  - ev: claude_realtime_filter.py:255-261 (in-prompt: score 50-70→WR73.7%, 120+→WR50% coin-flip); pump_detector.py:969,978,983,988,993,1000 etc. (running `score += N`); CONVICTION gate ranks/thresholds on final_score pump_detector.py:1995; screener best=max(scores) screener.py:3672
  - fix: Treat score as a NON-monotone eligibility band, not a rank. Empirically gate to the 50-90 sweet-spot (block <50 AND >120, not just <85). The current CONVICTION_MIN_SCORE=85 floor pushes selection INTO
- **[critical] VOL1** (catching_soundness): Validated volume edge is DISABLED; live patterns use the self-referential 5m vol_surge instead
  - ev: pump_detector.py:275 (USE_VALIDATED_V1=False) gates the dead vol_core block at 1247-1262; the live volume measure used by every pattern gate is vol_surge=volumes[-2]/mean(volumes[-12:-2]) at pump_detector.py:937-938, the self-referential 5m baseline (this session's KNOWN/FIXED note says it was fixed
  - fix: Wire vol_core.vol_factors/conviction into the LIVE additive score (not behind a False flag): replace or supplement the 5m vol_surge gate with vol_ratio_1h/4h vs 30-day median + vol_spike_count. This i
- **[high] BUG2** (correctness_bugs): daily_report time-window filter skewed by same naive-UTC-string bug for the screener CSV
  - ev: daily_report.py:90 cutoff=datetime.now(timezone.utc).timestamp() (true UTC epoch) vs :99 datetime.fromisoformat(ts_val.replace('Z','+00:00')).timestamp() — but resolved.csv run_ts has NO 'Z'/offset (confirmed '2026-04-12T09:56:48'), so fromisoformat yields a NAIVE datetime and .timestamp() assumes l
  - fix: Parse run_ts as UTC-aware (e.g. datetime.fromisoformat(ts).replace(tzinfo=timezone.utc) when no offset present) so the 'last N hours' cutoff is honest. On a +3h host, rows are mis-included/excluded by
- **[high] VAL2** (signal_validity): VALIDATED stablecoin_inflow boost (+20, lift 2.31) is commented out / unwired
  - ev: pump_detector.py:1269-1275 (entire +20 block commented; 'not yet wired'); only surfaced as macro text to Claude via macro_context.py:86
  - fix: Second-strongest validated signal (event-study lift 2.31) is dead in scoring. Wire stablecoin_flow.get_stable_flow() into scan_once pre-fetch and pass to _analyze_symbol so the +20 can apply. Until th
- **[high] VAL3** (signal_validity): VALIDATED btc_cascade for SHORTS (+30, lift 5.12) is DEAD in rug_detector
  - ev: rug_detector.py:548-552 (+30 if BTC 1h<=-1.5%) inside USE_VALIDATED_V1 block; flag=False @rug_detector.py:106 / gate :527
  - fix: btc_cascade is the single validated short-side edge (lift 5.12) and the only validated factor rug_detector has. It is gated off. The live rug score (rug_detector.py:490-521) rests entirely on unvalida
- **[high] DEB1** (signal_validity): DEBUNKED Fear&Greed STILL scored +10 in pump_detector (only live FNG scoring use)
  - ev: pump_detector.py:1286-1289 (+10 FNG_PUMP_FEAR_BONUS if FNG<25 & pump; +10 FNG_RUG_GREED_BONUS if FNG>75 & rug); constants 261-265
  - fix: macro_extreme/Fear&Greed is on the do-not-credit list (event-study lift 1.0). These +10s are added AFTER the MIN_SIGNAL_SCORE gate and before the min(score,130) clamp, so they can promote a borderline
- **[high] DEB2** (signal_validity): DEBUNKED whale-tracking: top-trader position skew +20/-15/+10 (pump_detector, post-gate)
  - ev: pump_detector.py:1428-1429 (+20 retail_LONG_top_SHORT), 1434-1435 (-15 retail_SHORT_top_LONG), 1439-1440 (+10 divergence); applied to r['score'] AFTER gate & AFTER min(score,130) clamp (clamp @1291)
  - fix: Whale-tracking is explicitly DEBUNKED. Worse, this edits score after the clamp and after the only score gate (re-gating gap), so a debunked +20 can push final_score over CONVICTION_MIN_SCORE(85) in wa
- **[high] DEB4** (signal_validity): DEBUNKED whale-tracking +22 and absorption in screener breakout
  - ev: screener.py:3288-3297 (+22 whale Buy / -12 Sell; +18 bull_absorb / -14 bear_absorb in breakout setup)
  - fix: +22 for a 'whale Buy' is one of breakout's larger levers and whale-tracking is DEBUNKED. Breakout is already the worst setup (40.9% WR) and is hard pre-filtered to SKIP at the RT gate (claude_realtime
- **[high] DEB5** (signal_validity): DEBUNKED SMC (FVG / Order Block) heavily scored in screener squeeze/bos_fvg/short_dist
  - ev: screener.py:2935-2951 (squeeze FVG +18 / OB +18 tiers), 3091-3103 (bos_fvg in-FVG +22 / in-OB +22, direction-agnostic), 3554-3584 (short_dist bear FVG +18 / bear OB +18)
  - fix: SMC (FVG/OB/BOS/ChoCH) is on the DEBUNKED list, yet FVG/OB are among the single heaviest additive contributions in three live setups (up to +22 each, stackable). bos_fvg is literally built on it. Thes
- **[high] DEB6** (signal_validity): DEBUNKED CHoCH used as a multiplier (x1.2) and additive in bos_fvg/breakout
  - ev: screener.py:3152-3155 (bos_fvg s2=int(s2*1.2) for bull OR bear CHoCH), 3402-3405 (breakout s4=int(s4*1.2)), 3033-3034 (squeeze -15 for bull_choch)
  - fix: ChoCH is SMC = DEBUNKED. The x1.2 multiplier is especially dangerous: it scales the ENTIRE setup score by 20% on a debunked condition, and is also leveraged in the TG gate via choch_conviction relaxin
- **[high] NOI1** (signal_validity): NOISE: OI triple-counted in screener (regime table + oi_div + magnitude/coil)
  - ev: screener.py:2890-2892 (squeeze _OI_SCORE regime) + 2895-2902 (oi_div +18/+8) ; breakout 3264-3267 (oi_coil +32) + 3308-3317 (oi_div +20/+12 THEN _OI_SCORE[breakout]); short_dist 3519-3529 (regime + oi_div)
  - fix: The _OI_SCORE table was the 'one-path' design to stop OI double-counting, but oi_div and oi_coiling are scored on TOP of it from correlated OI data — so OI is effectively counted 2-3x per setup. This 
- **[high] UNV1** (signal_validity): UNVALIDATED: funding-regime table is the dominant lever, never OOS-confirmed standalone
  - ev: screener.py:2357-2371 (_FUNDING_SCORE: pressure_building squeeze +60, high_longs_building short_dist +52); applied 2869/3180/3508
  - fix: The +60 pressure_building and +52 high_longs_building are the single largest additive contributions in the entire screener, yet funding-regime scoring is not on the validated list (only volume + stabl
- **[high] UNV2** (signal_validity): UNVALIDATED: pump_detector pattern base scores (the entire candidate set) are hand-tuned, untested
  - ev: pump_detector.py base scores SHORT_SQUEEZE +45 @969, POST_PUMP_RUG +50 @1046, VOLUME_SURGE +38 @1094, CVD_BULL/BEAR +40 @1117/1138, ATR_COIL +36 @1173, SLOW_DIST +40 @1208
  - fix: Every live pump/rug alert originates from one of these 7 hard-coded patterns whose base weights have no OOS validation; some (CVD_BULL/BEAR) are built on DEBUNKED CVD-divergence (see DEB7), VOLUME_SUR
- **[high] SCORE2** (scoring_soundness): OI is counted as a hard gate PLUS multiple additive bonuses in pump_detector — correlated reads summed as independent
  - ev: pump_detector.py:963 (OI gate for squeeze), :1043/:1061 (rug OI gate + OI-falling bonus +15), :1093 (volume_surge OI gate), :1121-1123 (cvd_bull OI confirm +10), :1142 (cvd_bear OI +10), :1182 (atr_coil OI4h +10), :1224 (slow_dist OI4h +15), plus cross-exchange Binance OI +15 @:1007-1010. Same OI va
  - fix: Pick ONE role for OI per pattern: either a binary gate OR a single graded bonus, never both. The Binance cross-exchange OI (+15) is ~collinear with Bybit oi_chg in normal regimes — do not add it as an
- **[high] SCORE3** (scoring_soundness): Liquidations triple-counted in pump_detector (Bybit-5m + Coinalyze-60m, overlapping windows) summed additively
  - ev: Squeeze: liq_short Bybit-5m +25 @pump_detector.py:992-994 THEN Coinalyze multi-exch +20/+10 @:997-1004 — the Coinalyze 60m window CONTAINS the Bybit 5m window, so the same liquidation cascade is scored twice (up to +45). Same pattern rug side: Bybit liq_long +30 @:1066-1068 + Coinalyze +20/+10 @:107
  - fix: Liquidations from different feeds over nested windows measure the SAME event. Take a single max() or de-duplicated USD figure and apply one tiered bonus. Currently a single big cascade can manufacture
- **[high] SCORE4** (scoring_soundness): screener 'Layer 3 one-path' only de-dups OI magnitude — oi_div and volume still enter ≥2x per setup
  - ev: Squeeze: _OI_SCORE[oi_regime]['squeeze'] @screener.py:2890 AND oi_div bonus +18/+8/-10 @:2895-2902 (two OI terms). Breakout: oi_div +20/+12/-10 @3308-3313 AND _OI_SCORE[oi_regime]['breakout'] @3314-3317 (two OI terms); plus vol_ratio +18/+10/+4 @3300-3305 stacks with vol_accel +26/-10 @3280-3285 (tw
  - fix: The regime-table refactor was the right idea but incomplete. Fold oi_div INTO the regime lookup (a single OI feature with sign), and fold vol_ratio + vol_accel into one volume feature (ratio with a di
- **[high] SCORE5** (scoring_soundness): Re-gating gap: post-gate, post-clamp edits mutate score with no second gate/clamp (both scorers)
  - ev: pump_detector: MIN_SIGNAL_SCORE=35 gate @1242 and min(score,130) clamp @1291 run, THEN scan_once edits r['score'] by pos-skew +20/-15/+10 @1429-1440 and orderbook ±10/+5 @1474-1514 with no re-clamp/re-gate — final score can exceed 130 and is built partly from DEBUNKED whale-tracking (pos_skew) and u
  - fix: Apply ALL score contributions BEFORE the selection/gate, then gate+clamp ONCE on the final value. If a contribution is allowed to change rank it must run before argmax; if it is only a confidence nudg
- **[high] FETCH1** (architecture_seams): Fetch seam is 4-way: pump_detector bare requests.get bypasses the FD-leak fix on the highest-volume hot paths
  - ev: pump_detector.py:619 (_fetch_kline_extremes), :792 (_fetch_oi_5m), :811 (_fetch_trades_raw), :1721 (_fetch_spot_prices), :1789 (_check_momentum) all use `import requests as _req` + bare _req.get with default keep-alive and NO Connection:close; vs screener.py:178-186 (SESSION with Connection:close + 
  - fix: Route pump_detector's bare _req.get calls through screener.SESSION (it already imports screener and uses it for klines/funding/tickers — mixed path proves it's available), or give pump_detector its ow
- **[high] CLAUDE1** (architecture_seams): claude_client singleton bypassed by channel_reader (2 sites) and signal_monitor (2 raw-HTTP sites)
  - ev: Singleton at claude_client.py:23-37 adopted by claude_realtime_filter.py:715-717, chart_analyzer.py:555-556, claude_analyst.py:220/295/404. BYPASSED: channel_reader.py:1836 and :509 each create `_ant.Anthropic(api_key=api_key)` per call; signal_monitor.py:97 and :668 use raw `requests.post('https://
  - fix: Migrate channel_reader's two call sites to claude_client.get_client(). For signal_monitor, either use get_client() or accept the raw-HTTP path is leak-free (no persistent SDK pool) but document it; at
- **[high] OUT1** (architecture_seams): outcome_model 'single source of truth' bypassed: migrate_pump_resolved_win_fix.py reimplements outcome() + hardcodes all thresholds
  - ev: migrate_pump_resolved_win_fix.py:36-46 is a verbatim copy of outcome_model.outcome() (outcome_model.py:21-35). Thresholds are hardcoded locally at :25-28 (PUMP_WIN_4H=12.0, PUMP_LOSS_4H=3.0, RUG_WIN_4H=10.0, RUG_LOSS_4H=3.0) and :30-33 (1h 3/2) — imports NOTHING from outcome_model. Currently magnitu
  - fix: Make migrate import PUMP_WIN_4H_PCT etc. and outcome() from outcome_model (handling the magnitude vs signed convention explicitly), so the recompute ruler can never drift from live/shadow.
- **[high] OUT2** (architecture_seams): 1h WIN/LOSS ruler is hardcoded magic numbers in 3 independent places — never sourced from outcome_model
  - ev: pump_detector.py:707-713 uses literals 1.03/0.98 (long) and 0.97/1.02 (short) for the 1h outcome; shadow_analyze.py:122-127 hardcodes the identical 1.03/0.98/0.97/1.02; migrate_pump_resolved_win_fix.py:30-33 hardcodes 3.0/2.0. outcome_model.py only defines 4h constants (15-18) — there are NO 1h cons
  - fix: Add PUMP_TP_1H_PCT/PUMP_SL_1H_PCT/RUG_TP_1H_PCT/RUG_SL_1H_PCT to outcome_model.py and import them in pump_detector + shadow_analyze + migrate.
- **[high] OUT3** (architecture_seams): daily_report mis-measures screener WR: counts only WIN/LOSS, silently dropping every TP1 and STOP row
  - ev: daily_report.py:104-110 increments wins only on outcome_4h=='WIN', losses only on 'LOSS', flat on 'FLAT' — TP1 and STOP are not counted in any bucket. _outcome_stats is applied to SCREENER_RESOLVED (daily_report.py:142, resolved.csv) which uses the 5-symbol vocabulary {TP1,STOP,WIN,LOSS,FLAT} (outco
  - fix: In daily_report._outcome_stats, map TP1→win and STOP→loss for the screener CSV (or branch by csv_path). As-is, the daily WR shown to the user understates the true screener population.
- **[high] DEAD1** (architecture_seams): USE_VALIDATED_V1 defined as two independent False flags (pump + rug) — the only OOS-validated edge (vol_core) is dead in production
  - ev: pump_detector.py:275 USE_VALIDATED_V1=False and rug_detector.py:106 USE_VALIDATED_V1=False are SEPARATE module-level constants (not a shared import). grep for `USE_VALIDATED_V1 = True` returns zero — never flipped anywhere. The pump V1 block (pump_detector.py:1247-1262) is the ONLY caller of vol_cor
  - fix: Hoist USE_VALIDATED_V1 into a shared config (or outcome_model/a config module) so one flag controls both paths. Decide whether the validated edge ships; right now the bot's only validated signal is in
- **[high] NOOP1** (architecture_seams): sweep_watcher live alert path is a no-op: sets setup='range_sweep' which the RT filter hard-SKIPs before any Claude call
  - ev: sweep_watcher.py:161 sets _cand['setup']='range_sweep' then calls claude_realtime_filter.filter_candidate (sweep_watcher.py:159), guarded by CLAUDE_RT_FILTER defaulting to 'on' (sweep_watcher.py:157). claude_realtime_filter.py:809-821 WEAK_SETUPS={breakout,swing,range_sweep} → instant SKIP, no Claud
  - fix: Either exempt the sweep source from the WEAK_SETUPS pre-filter (it is a different signal than screener 'breakout'/'swing'), or stop labeling it 'range_sweep' before the RT call, or document that sweep
- **[high] TIME1** (catching_soundness): Detect-early-then-chase architecture: momentum gate forces alerts to arrive AFTER the move starts
  - ev: Patterns fire on 'price hasn't moved' gates (squeeze price_chg_30m<=3.0 @962; VOLUME_SURGE abs(price_chg_30m)<=1.0 @1092; CVD_BULL price_chg_30m<=0.3 @1115), but send is blocked in _check_momentum until move_pct>=MOMENTUM_PCT(0.50%) @1781 AND 5m burst>=MOMENTUM_BURST_PCT(0.20%) @1817. So the candida
  - fix: Decide one philosophy. If catching early, drop/loosen the momentum-confirmation chase or make burst confirmation directional-magnitude-aware (e.g. require burst consistent with reaching the +12% targe
- **[high] ALIGN1** (catching_soundness): Catcher trigger scale (0.50% momentum) is ~24x smaller than the labeler WIN bar (+12%/4h, +15%/24h)
  - ev: Trigger MOMENTUM_PCT=0.50% @pump_detector.py:74 (applied 1781) and burst 0.20% @87; WIN ruler PUMP_WIN_4H_PCT=+12.0 @outcome_model.py:15 (applied tp_hit @pump_detector.py:732). No trigger or gate estimates expected magnitude near 12%/15%; the 'pump forming' notion is 'started drifting 0.5%', the lab
  - fix: Introduce an expected-magnitude estimate into the trigger (e.g. from vol_core tier + ATR-coil expansion potential) and require the candidate's plausible 4h move to approach the WIN threshold before se
- **[high] SCORE1** (catching_soundness): Conviction gate operates on an anti-predictive additive score; high-score selection picks statistically worse pumps
  - ev: First gate MIN_SIGNAL_SCORE=35 @pump_detector.py:1242, second gate CONVICTION_MIN_SCORE=85 on final_score @1995; clamp min(score,130) @1291 happens BEFORE post-scan orderbook/skew/DEX edits (1428-1515, 1989) so the clamp is bypassed and there is no re-gate. Established finding: score 50-70→WR~74% vs
  - fix: Revert CONVICTION_MIN_SCORE to 65 (the comment at :118 already plans this) AND, more importantly, stop using raw additive score as the conviction proxy. Replace with the vol_core tier + a small set of